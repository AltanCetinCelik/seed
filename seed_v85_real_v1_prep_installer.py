#!/usr/bin/env python3
from pathlib import Path
import json
import re
import subprocess
import sys

MODULES = json.loads("{\"seed_self_state_v85.py\": \"\\nimport json\\nfrom datetime import datetime\\nfrom pathlib import Path\\n\\nSTATE_FILE = Path(\\\"seed_self_state_v85.json\\\")\\n\\ndef now():\\n    return datetime.now().isoformat(timespec=\\\"seconds\\\")\\n\\ndef read_json(path, default=None):\\n    p = Path(path)\\n    if not p.exists():\\n        return default if default is not None else {}\\n    try:\\n        return json.loads(p.read_text(errors=\\\"ignore\\\"))\\n    except Exception:\\n        return default if default is not None else {}\\n\\ndef safe(fn, fallback=None):\\n    try:\\n        return fn()\\n    except Exception as e:\\n        return fallback if fallback is not None else {\\\"ok\\\": False, \\\"error\\\": str(e)}\\n\\ndef build_self_state():\\n    # No gate calls here. This is deliberately lightweight to avoid recursion and slow chat.\\n    reports = {\\n        \\\"v81\\\": read_json(\\\"seed_v81_gate_report.json\\\", {}),\\n        \\\"v75\\\": read_json(\\\"seed_v75_gate_report.json\\\", {}),\\n        \\\"v74\\\": read_json(\\\"seed_v74_gate_report.json\\\", {}),\\n        \\\"v731\\\": read_json(\\\"seed_v731_gate_report.json\\\", {}),\\n    }\\n\\n    role_map, models = {}, []\\n    try:\\n        from seed_model_real_mode_v61 import load_role_map, list_models\\n        role_map = load_role_map().get(\\\"role_map\\\", {})\\n        models = list_models().get(\\\"models\\\", [])\\n    except Exception:\\n        pass\\n\\n    memory = safe(lambda: __import__(\\\"seed_memory_review_v75\\\", fromlist=[\\\"memory_summary\\\"]).memory_summary(), {})\\n    recovery = safe(lambda: __import__(\\\"seed_recovery_v82\\\", fromlist=[\\\"recovery_summary\\\"]).recovery_summary(), {})\\n    runtime = safe(lambda: __import__(\\\"seed_runtime_v83\\\", fromlist=[\\\"runtime_status\\\"]).runtime_status(), {})\\n    privacy = safe(lambda: __import__(\\\"seed_privacy_backup_v84\\\", fromlist=[\\\"privacy_status\\\"]).privacy_status(), {})\\n    rc = safe(lambda: __import__(\\\"seed_release_candidate_v85\\\", fromlist=[\\\"release_candidate_summary\\\"]).release_candidate_summary(), {})\\n\\n    installed = [\\\"v85\\\", \\\"v84\\\", \\\"v83\\\", \\\"v82\\\", \\\"v81\\\", \\\"v80\\\", \\\"v79\\\", \\\"v78\\\", \\\"v77\\\", \\\"v76\\\"]\\n    for key in [\\\"v75\\\", \\\"v74\\\", \\\"v731\\\"]:\\n        if reports.get(key, {}).get(\\\"ready\\\"):\\n            installed.append(key)\\n    installed.extend([\\\"v73\\\", \\\"v72\\\", \\\"v70\\\"])\\n\\n    data = {\\n        \\\"created_at\\\": now(),\\n        \\\"version\\\": \\\"v85.0.0\\\",\\n        \\\"ok\\\": True,\\n        \\\"true_current_version\\\": \\\"v85.0.0\\\",\\n        \\\"release_track\\\": \\\"Seed v1 release candidate preparation\\\",\\n        \\\"installed_layers_green\\\": installed,\\n        \\\"report_snapshot\\\": {k: {\\\"ready\\\": v.get(\\\"ready\\\"), \\\"version\\\": v.get(\\\"version\\\")} for k, v in reports.items()},\\n        \\\"capabilities\\\": {\\n            \\\"recovery_self_repair\\\": True,\\n            \\\"one_command_runtime\\\": True,\\n            \\\"backup_privacy_export_forget\\\": True,\\n            \\\"release_candidate_checks\\\": True,\\n            \\\"v81_mega_stack\\\": reports.get(\\\"v81\\\", {}).get(\\\"ready\\\") is True,\\n            \\\"real_memory_review\\\": bool(memory.get(\\\"ok\\\", True)),\\n        },\\n        \\\"models\\\": models,\\n        \\\"role_map\\\": role_map,\\n        \\\"memory\\\": memory,\\n        \\\"recovery\\\": recovery,\\n        \\\"runtime\\\": runtime,\\n        \\\"privacy\\\": privacy,\\n        \\\"release_candidate\\\": rc,\\n        \\\"truth_rules\\\": [\\n            \\\"Current version is v85.0.0 when v85 gate is green.\\\",\\n            \\\"v82-v85 were installed as the final real-v1 preparation stack.\\\",\\n            \\\"v81 and below are older green layers, not the current version.\\\",\\n            \\\"Remaining step after v85 is v1.0 final stabilization.\\\",\\n        ],\\n        \\\"next_recommended_updates\\\": [\\n            \\\"Fix anything v85 RC report marks as blocker.\\\",\\n            \\\"Run one-command runtime and backup/privacy tests.\\\",\\n            \\\"Prepare Seed v1.0 final release.\\\",\\n        ],\\n    }\\n\\n    STATE_FILE.write_text(json.dumps(data, indent=4, ensure_ascii=False))\\n    return data\\n\\ndef build_seed_truth_context():\\n    s = build_self_state()\\n    return \\\"\\\\n\\\".join([\\n        \\\"=== TRUE CURRENT SEED STATE OVERRIDE ===\\\",\\n        f\\\"Current Seed version: {s.get('true_current_version')}\\\",\\n        \\\"Current stage: Seed v1 release-candidate preparation.\\\",\\n        f\\\"Green/current layers: {', '.join(s.get('installed_layers_green', []))}\\\",\\n        f\\\"Recovery/self-repair available: {s['capabilities'].get('recovery_self_repair')}\\\",\\n        f\\\"One-command runtime available: {s['capabilities'].get('one_command_runtime')}\\\",\\n        f\\\"Backup/privacy/export/forget available: {s['capabilities'].get('backup_privacy_export_forget')}\\\",\\n        f\\\"Release candidate checks available: {s['capabilities'].get('release_candidate_checks')}\\\",\\n        \\\"Do not call v70/v75/v81 the current version. They are older green layers.\\\",\\n        \\\"Next best work after v85: run RC checks, fix blockers, then Seed v1.0 final.\\\",\\n        \\\"========================================\\\",\\n    ])\\n\\ndef show_self_state():\\n    print(\\\"\\\\n=== SEED v85 SELF-STATE TRUTH ===\\\")\\n    print(json.dumps(build_self_state(), indent=4, ensure_ascii=False))\\n\\nif __name__ == \\\"__main__\\\":\\n    show_self_state()\\n\", \"seed_recovery_v82.py\": \"\\nimport json\\nimport os\\nimport re\\nimport subprocess\\nimport sys\\nfrom datetime import datetime\\nfrom pathlib import Path\\n\\nREPORT_FILE = Path(\\\"seed_recovery_report_v82.json\\\")\\nLAST_GREEN_FILE = Path(\\\"seed_last_green_checkpoint_v82.json\\\")\\nROLLBACK_NOTES = Path(\\\"seed_recovery_rollback_notes_v82.md\\\")\\n\\ndef now():\\n    return datetime.now().isoformat(timespec=\\\"seconds\\\")\\n\\ndef seed_py_files(limit=None):\\n    files = sorted(Path(\\\".\\\").glob(\\\"seed_*.py\\\"))\\n    if limit:\\n        files = files[:limit]\\n    return files\\n\\ndef compile_file(path, timeout=25):\\n    try:\\n        proc = subprocess.run([sys.executable, \\\"-m\\\", \\\"py_compile\\\", str(path)], capture_output=True, text=True, timeout=timeout)\\n        return {\\\"file\\\": str(path), \\\"ok\\\": proc.returncode == 0, \\\"stderr\\\": proc.stderr[-2000:]}\\n    except subprocess.TimeoutExpired:\\n        return {\\\"file\\\": str(path), \\\"ok\\\": False, \\\"stderr\\\": \\\"compile timeout\\\"}\\n    except Exception as e:\\n        return {\\\"file\\\": str(path), \\\"ok\\\": False, \\\"stderr\\\": str(e)}\\n\\ndef compile_all():\\n    checks = [compile_file(p) for p in seed_py_files()]\\n    return {\\n        \\\"ok\\\": all(c[\\\"ok\\\"] for c in checks),\\n        \\\"count\\\": len(checks),\\n        \\\"failed\\\": [c for c in checks if not c[\\\"ok\\\"]],\\n        \\\"checks\\\": checks,\\n    }\\n\\ndef run_gate_command(command, timeout=80):\\n    try:\\n        proc = subprocess.run(command, capture_output=True, text=True, timeout=timeout)\\n        return {\\n            \\\"command\\\": \\\" \\\".join(command),\\n            \\\"ok\\\": proc.returncode == 0,\\n            \\\"returncode\\\": proc.returncode,\\n            \\\"stdout_tail\\\": proc.stdout[-4000:],\\n            \\\"stderr_tail\\\": proc.stderr[-4000:],\\n        }\\n    except subprocess.TimeoutExpired:\\n        return {\\\"command\\\": \\\" \\\".join(command), \\\"ok\\\": False, \\\"returncode\\\": None, \\\"stderr_tail\\\": f\\\"timeout after {timeout}s\\\"}\\n    except Exception as e:\\n        return {\\\"command\\\": \\\" \\\".join(command), \\\"ok\\\": False, \\\"returncode\\\": None, \\\"stderr_tail\\\": str(e)}\\n\\ndef recursion_guard():\\n    warnings = []\\n    patterns = [\\n        (\\\"seed_self_state_v741.py\\\", \\\"seed_v75_gate\\\", \\\"v75 self-state should not call v75 gate\\\"),\\n        (\\\"seed_self_state_v81.py\\\", \\\"seed_v81_gate\\\", \\\"v81 self-state should not call v81 gate\\\"),\\n        (\\\"seed_self_state_v85.py\\\", \\\"seed_v85_gate\\\", \\\"v85 self-state should not call v85 gate\\\"),\\n    ]\\n    for filename, needle, message in patterns:\\n        p = Path(filename)\\n        if p.exists() and needle in p.read_text(errors=\\\"ignore\\\"):\\n            warnings.append({\\\"file\\\": filename, \\\"warning\\\": message})\\n    return {\\\"ok\\\": len(warnings) == 0, \\\"warnings\\\": warnings}\\n\\ndef dependency_check():\\n    deps = {}\\n    for name in [\\\"ffmpeg\\\", \\\"say\\\", \\\"aider\\\", \\\"git\\\"]:\\n        try:\\n            import shutil\\n            deps[name] = shutil.which(name)\\n        except Exception:\\n            deps[name] = None\\n    try:\\n        import faster_whisper\\n        deps[\\\"faster_whisper\\\"] = True\\n    except Exception:\\n        deps[\\\"faster_whisper\\\"] = False\\n    return {\\\"ok\\\": bool(deps.get(\\\"git\\\")), \\\"deps\\\": deps}\\n\\ndef storage_check():\\n    try:\\n        usage = os.statvfs(\\\".\\\")\\n        free_gb = usage.f_bavail * usage.f_frsize / (1024**3)\\n        total_gb = usage.f_blocks * usage.f_frsize / (1024**3)\\n        return {\\\"ok\\\": free_gb > 5, \\\"free_gb\\\": round(free_gb, 2), \\\"total_gb\\\": round(total_gb, 2)}\\n    except Exception as e:\\n        return {\\\"ok\\\": False, \\\"error\\\": str(e)}\\n\\ndef mark_last_green(label=\\\"manual_green_checkpoint\\\"):\\n    data = {\\n        \\\"created_at\\\": now(),\\n        \\\"version\\\": \\\"v82.0.0\\\",\\n        \\\"label\\\": label,\\n        \\\"cwd\\\": str(Path(\\\".\\\").resolve()),\\n    }\\n    try:\\n        proc = subprocess.run([\\\"git\\\", \\\"rev-parse\\\", \\\"--short\\\", \\\"HEAD\\\"], capture_output=True, text=True, timeout=8)\\n        if proc.returncode == 0:\\n            data[\\\"git_head\\\"] = proc.stdout.strip()\\n    except Exception:\\n        pass\\n    LAST_GREEN_FILE.write_text(json.dumps(data, indent=4))\\n    return data\\n\\ndef recovery_summary():\\n    compile_report = compile_all()\\n    guard = recursion_guard()\\n    deps = dependency_check()\\n    storage = storage_check()\\n    blockers = []\\n    if not compile_report[\\\"ok\\\"]:\\n        blockers.append(f\\\"{len(compile_report['failed'])} Python compile failures\\\")\\n    if not guard[\\\"ok\\\"]:\\n        blockers.append(\\\"recursion guard warnings\\\")\\n    if not deps[\\\"ok\\\"]:\\n        blockers.append(\\\"missing critical dependency\\\")\\n    if not storage[\\\"ok\\\"]:\\n        blockers.append(\\\"low storage or storage check failed\\\")\\n    data = {\\n        \\\"created_at\\\": now(),\\n        \\\"version\\\": \\\"v82.0.0\\\",\\n        \\\"ok\\\": len(blockers) == 0,\\n        \\\"blockers\\\": blockers,\\n        \\\"compile\\\": {\\\"ok\\\": compile_report[\\\"ok\\\"], \\\"count\\\": compile_report[\\\"count\\\"], \\\"failed\\\": compile_report[\\\"failed\\\"][:10]},\\n        \\\"recursion_guard\\\": guard,\\n        \\\"dependencies\\\": deps,\\n        \\\"storage\\\": storage,\\n        \\\"last_green\\\": json.loads(LAST_GREEN_FILE.read_text()) if LAST_GREEN_FILE.exists() else None,\\n    }\\n    REPORT_FILE.write_text(json.dumps(data, indent=4, ensure_ascii=False))\\n    return data\\n\\ndef write_rollback_notes():\\n    summary = recovery_summary()\\n    text = [\\n        \\\"# Seed v82 Recovery Notes\\\",\\n        \\\"\\\",\\n        f\\\"Created: {now()}\\\",\\n        \\\"\\\",\\n        \\\"## Status\\\",\\n        f\\\"OK: {summary.get('ok')}\\\",\\n        f\\\"Blockers: {summary.get('blockers')}\\\",\\n        \\\"\\\",\\n        \\\"## Manual rollback strategy\\\",\\n        \\\"1. Stop running Seed servers with Ctrl+C.\\\",\\n        \\\"2. Use the latest `seed_backup_*_green` folder on Desktop.\\\",\\n        \\\"3. If git is clean, use `git log --oneline` then `git reset --hard <green_commit>`.\\\",\\n        \\\"4. Re-run gates after rollback.\\\",\\n        \\\"\\\",\\n        \\\"## Current failed compiles\\\",\\n    ]\\n    for fail in summary.get(\\\"compile\\\", {}).get(\\\"failed\\\", []):\\n        text.append(f\\\"- {fail.get('file')}: {fail.get('stderr')[:400]}\\\")\\n    ROLLBACK_NOTES.write_text(\\\"\\\\n\\\".join(text))\\n    return {\\\"ok\\\": True, \\\"file\\\": str(ROLLBACK_NOTES)}\\n\\ndef show_recovery():\\n    print(\\\"\\\\n=== SEED v82 RECOVERY / SELF-REPAIR ===\\\")\\n    data = recovery_summary()\\n    print(f\\\"OK: {data['ok']}\\\")\\n    print(f\\\"Blockers: {data['blockers']}\\\")\\n    print(f\\\"Compile: {data['compile']['ok']} ({data['compile']['count']} files)\\\")\\n    print(f\\\"Storage: {data['storage']}\\\")\\n    print(f\\\"Deps: {data['dependencies']['deps']}\\\")\\n    if data[\\\"compile\\\"][\\\"failed\\\"]:\\n        print(\\\"\\\\nFailed compiles:\\\")\\n        for f in data[\\\"compile\\\"][\\\"failed\\\"]:\\n            print(f\\\"- {f['file']}: {f['stderr'][:500]}\\\")\\n    print(\\\"\\\\nUse: mark green checkpoint | recovery notes\\\")\\n\\nif __name__ == \\\"__main__\\\":\\n    show_recovery()\\n\", \"seed_runtime_v83.py\": \"\\nimport json\\nimport os\\nimport signal\\nimport subprocess\\nimport sys\\nimport time\\nfrom datetime import datetime\\nfrom pathlib import Path\\n\\nSTATE_FILE = Path(\\\"seed_runtime_v83_state.json\\\")\\nLOG_FILE = Path(\\\"seed_runtime_v83_panel.log\\\")\\nPID_FILE = Path(\\\"seed_runtime_v83_panel.pid\\\")\\n\\ndef now():\\n    return datetime.now().isoformat(timespec=\\\"seconds\\\")\\n\\ndef pid_alive(pid):\\n    try:\\n        os.kill(int(pid), 0)\\n        return True\\n    except Exception:\\n        return False\\n\\ndef runtime_status():\\n    pid = None\\n    if PID_FILE.exists():\\n        try:\\n            pid = int(PID_FILE.read_text().strip())\\n        except Exception:\\n            pid = None\\n    panel_alive = bool(pid and pid_alive(pid))\\n    data = {\\n        \\\"created_at\\\": now(),\\n        \\\"version\\\": \\\"v83.0.0\\\",\\n        \\\"ok\\\": True,\\n        \\\"panel_pid\\\": pid,\\n        \\\"panel_alive\\\": panel_alive,\\n        \\\"panel_url\\\": \\\"http://127.0.0.1:8797\\\",\\n        \\\"script\\\": \\\"./seed\\\",\\n        \\\"fallback_script\\\": \\\"./seed_start.sh\\\",\\n        \\\"log_file\\\": str(LOG_FILE),\\n    }\\n    STATE_FILE.write_text(json.dumps(data, indent=4))\\n    return data\\n\\ndef run_quick_gate():\\n    for gate in [\\\"seed_v85_gate.py\\\", \\\"seed_v81_gate.py\\\", \\\"seed_v75_gate.py\\\"]:\\n        if Path(gate).exists():\\n            try:\\n                proc = subprocess.run([sys.executable, gate], capture_output=True, text=True, timeout=120)\\n                print(proc.stdout[-4000:])\\n                if proc.returncode != 0:\\n                    print(proc.stderr[-4000:])\\n                    return False\\n                return True\\n            except Exception as e:\\n                print(f\\\"Gate failed: {e}\\\")\\n                return False\\n    print(\\\"No gate found.\\\")\\n    return False\\n\\ndef start_panel(open_browser=True):\\n    if PID_FILE.exists():\\n        try:\\n            old_pid = int(PID_FILE.read_text().strip())\\n            if pid_alive(old_pid):\\n                print(f\\\"Panel already running: http://127.0.0.1:8797 pid={old_pid}\\\")\\n                return {\\\"ok\\\": True, \\\"pid\\\": old_pid, \\\"already_running\\\": True}\\n        except Exception:\\n            pass\\n\\n    panel_file = \\\"seed_panel_v77.py\\\" if Path(\\\"seed_panel_v77.py\\\").exists() else \\\"seed_embodied_companion_server_v74.py\\\"\\n    log = LOG_FILE.open(\\\"a\\\")\\n    proc = subprocess.Popen([sys.executable, panel_file], stdout=log, stderr=log)\\n    PID_FILE.write_text(str(proc.pid))\\n    time.sleep(1)\\n    if open_browser:\\n        try:\\n            subprocess.Popen([\\\"open\\\", \\\"http://127.0.0.1:8797\\\"])\\n        except Exception:\\n            pass\\n    print(f\\\"Started Seed panel pid={proc.pid}\\\")\\n    print(\\\"Open: http://127.0.0.1:8797\\\")\\n    return {\\\"ok\\\": True, \\\"pid\\\": proc.pid, \\\"url\\\": \\\"http://127.0.0.1:8797\\\"}\\n\\ndef stop_panel():\\n    if not PID_FILE.exists():\\n        print(\\\"No panel PID file.\\\")\\n        return {\\\"ok\\\": True, \\\"stopped\\\": False}\\n    try:\\n        pid = int(PID_FILE.read_text().strip())\\n        if pid_alive(pid):\\n            os.kill(pid, signal.SIGTERM)\\n            time.sleep(0.5)\\n        PID_FILE.unlink(missing_ok=True)\\n        print(f\\\"Stopped panel pid={pid}\\\")\\n        return {\\\"ok\\\": True, \\\"stopped\\\": True, \\\"pid\\\": pid}\\n    except Exception as e:\\n        print(f\\\"Stop failed: {e}\\\")\\n        return {\\\"ok\\\": False, \\\"error\\\": str(e)}\\n\\ndef seed_start(run_gate=False):\\n    print(\\\"\\\\n=== SEED v83 ONE-COMMAND RUNTIME ===\\\")\\n    print(\\\"Starting Seed local companion runtime...\\\")\\n    if run_gate:\\n        print(\\\"Running quick gate first...\\\")\\n        run_quick_gate()\\n    return start_panel(open_browser=True)\\n\\ndef install_seed_script():\\n    script = Path(\\\"seed\\\")\\n    script.write_text(\\\"\\\"\\\"#!/usr/bin/env bash\\ncd \\\"$(dirname \\\"$0\\\")\\\"\\npython seed_runtime_v83.py start \\\"$@\\\"\\n\\\"\\\"\\\")\\n    script.chmod(0o755)\\n    fallback = Path(\\\"seed_start.sh\\\")\\n    fallback.write_text(\\\"\\\"\\\"#!/usr/bin/env bash\\ncd \\\"$(dirname \\\"$0\\\")\\\"\\npython seed_runtime_v83.py start \\\"$@\\\"\\n\\\"\\\"\\\")\\n    fallback.chmod(0o755)\\n    return {\\\"ok\\\": True, \\\"script\\\": str(script), \\\"fallback\\\": str(fallback)}\\n\\ndef show_runtime():\\n    print(\\\"\\\\n=== SEED v83 ONE-COMMAND RUNTIME ===\\\")\\n    print(json.dumps(runtime_status(), indent=4))\\n    print(\\\"\\\\nCommands:\\\")\\n    print(\\\"./seed\\\")\\n    print(\\\"python seed_runtime_v83.py start\\\")\\n    print(\\\"python seed_runtime_v83.py stop\\\")\\n    print(\\\"python seed_runtime_v83.py status\\\")\\n\\nif __name__ == \\\"__main__\\\":\\n    arg = sys.argv[1] if len(sys.argv) > 1 else \\\"status\\\"\\n    if arg == \\\"start\\\":\\n        seed_start(run_gate=\\\"--gate\\\" in sys.argv)\\n    elif arg == \\\"stop\\\":\\n        stop_panel()\\n    elif arg == \\\"install-script\\\":\\n        print(install_seed_script())\\n    else:\\n        show_runtime()\\n\", \"seed_privacy_backup_v84.py\": \"\\nimport json\\nimport shutil\\nimport zipfile\\nfrom datetime import datetime\\nfrom pathlib import Path\\n\\nBACKUP_DIR = Path(\\\"seed_backups_v84\\\")\\nEXPORT_DIR = Path(\\\"seed_exports_v84\\\")\\nFORGET_LOG = Path(\\\"seed_forget_log_v84.jsonl\\\")\\n\\nEXCLUDE_DIRS = {\\\".git\\\", \\\"__pycache__\\\", \\\"seed_voice_recordings_v731\\\", \\\"seed_backups_v84\\\", \\\"node_modules\\\", \\\".venv\\\", \\\"venv\\\"}\\nEXCLUDE_SUFFIXES = {\\\".pyc\\\", \\\".wav\\\", \\\".mp3\\\", \\\".mp4\\\", \\\".mov\\\"}\\n\\ndef now():\\n    return datetime.now().isoformat(timespec=\\\"seconds\\\")\\n\\ndef ensure_dirs():\\n    BACKUP_DIR.mkdir(exist_ok=True)\\n    EXPORT_DIR.mkdir(exist_ok=True)\\n\\ndef should_skip(path):\\n    parts = set(path.parts)\\n    if parts & EXCLUDE_DIRS:\\n        return True\\n    if path.suffix.lower() in EXCLUDE_SUFFIXES:\\n        return True\\n    return False\\n\\ndef backup_project(label=\\\"manual\\\"):\\n    ensure_dirs()\\n    stamp = datetime.now().strftime(\\\"%Y%m%d_%H%M%S\\\")\\n    out = BACKUP_DIR / f\\\"seed_backup_{stamp}_{label}.zip\\\"\\n    count = 0\\n    with zipfile.ZipFile(out, \\\"w\\\", zipfile.ZIP_DEFLATED) as z:\\n        for p in Path(\\\".\\\").rglob(\\\"*\\\"):\\n            if p.is_file() and not should_skip(p):\\n                z.write(p, p.as_posix())\\n                count += 1\\n    meta = {\\\"created_at\\\": now(), \\\"version\\\": \\\"v84.0.0\\\", \\\"ok\\\": True, \\\"file\\\": str(out), \\\"files\\\": count}\\n    (BACKUP_DIR / f\\\"{out.stem}.json\\\").write_text(json.dumps(meta, indent=4))\\n    return meta\\n\\ndef list_backups():\\n    ensure_dirs()\\n    items = []\\n    for p in sorted(BACKUP_DIR.glob(\\\"seed_backup_*.zip\\\")):\\n        items.append({\\\"file\\\": str(p), \\\"size_mb\\\": round(p.stat().st_size / (1024*1024), 2), \\\"created\\\": datetime.fromtimestamp(p.stat().st_mtime).isoformat(timespec=\\\"seconds\\\")})\\n    return {\\\"ok\\\": True, \\\"count\\\": len(items), \\\"items\\\": items[-20:]}\\n\\ndef export_memory():\\n    ensure_dirs()\\n    stamp = datetime.now().strftime(\\\"%Y%m%d_%H%M%S\\\")\\n    json_out = EXPORT_DIR / f\\\"seed_memory_export_{stamp}.json\\\"\\n    txt_out = EXPORT_DIR / f\\\"seed_memory_export_{stamp}.txt\\\"\\n\\n    data = {}\\n    for file in [\\\"seed_long_term_memory_v75.json\\\", \\\"seed_memory_decisions_v75.jsonl\\\", \\\"seed_self_state_v85.json\\\", \\\"seed_self_state_v81.json\\\"]:\\n        p = Path(file)\\n        if p.exists():\\n            if p.suffix == \\\".json\\\":\\n                try:\\n                    data[file] = json.loads(p.read_text(errors=\\\"ignore\\\"))\\n                except Exception:\\n                    data[file] = p.read_text(errors=\\\"ignore\\\")\\n            else:\\n                data[file] = p.read_text(errors=\\\"ignore\\\").splitlines()\\n\\n    json_out.write_text(json.dumps({\\\"created_at\\\": now(), \\\"version\\\": \\\"v84.0.0\\\", \\\"data\\\": data}, indent=4, ensure_ascii=False))\\n\\n    lines = [f\\\"Seed memory export {now()}\\\", \\\"\\\"]\\n    mem = data.get(\\\"seed_long_term_memory_v75.json\\\", {}).get(\\\"memories\\\", []) if isinstance(data.get(\\\"seed_long_term_memory_v75.json\\\"), dict) else []\\n    for item in mem:\\n        lines.append(f\\\"- {item.get('id')}: {item.get('text')}\\\")\\n    txt_out.write_text(\\\"\\\\n\\\".join(lines))\\n    return {\\\"ok\\\": True, \\\"json\\\": str(json_out), \\\"txt\\\": str(txt_out), \\\"memory_count\\\": len(mem)}\\n\\ndef forget_keyword(keyword):\\n    keyword = str(keyword or \\\"\\\").strip()\\n    if not keyword:\\n        return {\\\"ok\\\": False, \\\"error\\\": \\\"empty keyword\\\"}\\n    p = Path(\\\"seed_long_term_memory_v75.json\\\")\\n    if not p.exists():\\n        return {\\\"ok\\\": False, \\\"error\\\": \\\"memory file not found\\\"}\\n\\n    data = json.loads(p.read_text(errors=\\\"ignore\\\"))\\n    before = data.get(\\\"memories\\\", [])\\n    kept, removed = [], []\\n    for item in before:\\n        if keyword.lower() in str(item.get(\\\"text\\\", \\\"\\\")).lower():\\n            removed.append(item)\\n        else:\\n            kept.append(item)\\n    data[\\\"memories\\\"] = kept\\n    data[\\\"updated_at\\\"] = now()\\n    p.write_text(json.dumps(data, indent=4, ensure_ascii=False))\\n\\n    with FORGET_LOG.open(\\\"a\\\") as f:\\n        f.write(json.dumps({\\\"created_at\\\": now(), \\\"version\\\": \\\"v84.0.0\\\", \\\"keyword\\\": keyword, \\\"removed\\\": removed}, ensure_ascii=False) + \\\"\\\\n\\\")\\n\\n    return {\\\"ok\\\": True, \\\"keyword\\\": keyword, \\\"removed_count\\\": len(removed), \\\"kept_count\\\": len(kept)}\\n\\ndef cleanup_voice_logs():\\n    # Non-destructive by default: only counts.\\n    files = list(Path(\\\".\\\").glob(\\\"seed_voice_recordings_v731/*.wav\\\"))\\n    return {\\\"ok\\\": True, \\\"recording_count\\\": len(files), \\\"note\\\": \\\"Non-destructive. Delete manually or add destructive cleanup later.\\\"}\\n\\ndef privacy_status():\\n    ensure_dirs()\\n    exported = list(EXPORT_DIR.glob(\\\"*\\\"))\\n    backups = list(BACKUP_DIR.glob(\\\"*.zip\\\"))\\n    memory_file = Path(\\\"seed_long_term_memory_v75.json\\\")\\n    memory_count = 0\\n    if memory_file.exists():\\n        try:\\n            memory_count = len(json.loads(memory_file.read_text(errors=\\\"ignore\\\")).get(\\\"memories\\\", []))\\n        except Exception:\\n            pass\\n    return {\\n        \\\"created_at\\\": now(),\\n        \\\"version\\\": \\\"v84.0.0\\\",\\n        \\\"ok\\\": True,\\n        \\\"backup_count\\\": len(backups),\\n        \\\"export_count\\\": len(exported),\\n        \\\"memory_count\\\": memory_count,\\n        \\\"forget_log_exists\\\": FORGET_LOG.exists(),\\n        \\\"dirs\\\": {\\\"backups\\\": str(BACKUP_DIR), \\\"exports\\\": str(EXPORT_DIR)},\\n    }\\n\\ndef show_privacy():\\n    print(\\\"\\\\n=== SEED v84 BACKUP / PRIVACY / EXPORT ===\\\")\\n    print(json.dumps(privacy_status(), indent=4, ensure_ascii=False))\\n    print(\\\"\\\\nCommands:\\\")\\n    print(\\\"backup seed\\\")\\n    print(\\\"list backups\\\")\\n    print(\\\"export memory\\\")\\n    print(\\\"forget memory <keyword>\\\")\\n\\nif __name__ == \\\"__main__\\\":\\n    show_privacy()\\n\", \"seed_release_candidate_v85.py\": \"\\nimport json\\nimport subprocess\\nimport sys\\nfrom datetime import datetime\\nfrom pathlib import Path\\n\\nREPORT_FILE = Path(\\\"seed_v85_release_candidate_report.json\\\")\\nREADME_FILE = Path(\\\"README_SEED_V1_RC.md\\\")\\n\\nESSENTIAL_FILES = [\\n    \\\"seed_cli.py\\\",\\n    \\\"seed_local_chat_v701.py\\\",\\n    \\\"seed_v81_gate.py\\\",\\n    \\\"seed_v85_gate.py\\\",\\n    \\\"seed_runtime_v83.py\\\",\\n    \\\"seed_privacy_backup_v84.py\\\",\\n    \\\"seed_recovery_v82.py\\\",\\n]\\n\\ndef now():\\n    return datetime.now().isoformat(timespec=\\\"seconds\\\")\\n\\ndef run(cmd, timeout=120):\\n    try:\\n        proc = subprocess.run(cmd, capture_output=True, text=True, timeout=timeout)\\n        return {\\\"command\\\": \\\" \\\".join(cmd), \\\"ok\\\": proc.returncode == 0, \\\"returncode\\\": proc.returncode, \\\"stdout_tail\\\": proc.stdout[-4000:], \\\"stderr_tail\\\": proc.stderr[-4000:]}\\n    except subprocess.TimeoutExpired:\\n        return {\\\"command\\\": \\\" \\\".join(cmd), \\\"ok\\\": False, \\\"returncode\\\": None, \\\"stderr_tail\\\": f\\\"timeout after {timeout}s\\\"}\\n    except Exception as e:\\n        return {\\\"command\\\": \\\" \\\".join(cmd), \\\"ok\\\": False, \\\"returncode\\\": None, \\\"stderr_tail\\\": str(e)}\\n\\ndef essential_file_check():\\n    items = []\\n    for f in ESSENTIAL_FILES:\\n        p = Path(f)\\n        items.append({\\\"file\\\": f, \\\"exists\\\": p.exists(), \\\"size\\\": p.stat().st_size if p.exists() else 0})\\n    return {\\\"ok\\\": all(i[\\\"exists\\\"] for i in items), \\\"items\\\": items}\\n\\ndef compile_core():\\n    files = [p for p in Path(\\\".\\\").glob(\\\"seed_*.py\\\")]\\n    checks = []\\n    for p in files:\\n        proc = run([sys.executable, \\\"-m\\\", \\\"py_compile\\\", str(p)], timeout=30)\\n        checks.append({\\\"file\\\": str(p), \\\"ok\\\": proc[\\\"ok\\\"], \\\"stderr\\\": proc.get(\\\"stderr_tail\\\", \\\"\\\")})\\n    return {\\\"ok\\\": all(c[\\\"ok\\\"] for c in checks), \\\"count\\\": len(checks), \\\"failed\\\": [c for c in checks if not c[\\\"ok\\\"]][:20]}\\n\\ndef gate_simulation():\\n    gates = []\\n    for g in [\\\"seed_v81_gate.py\\\", \\\"seed_v75_gate.py\\\", \\\"seed_v74_gate.py\\\", \\\"seed_v731_gate.py\\\"]:\\n        if Path(g).exists():\\n            gates.append(run([sys.executable, g], timeout=160))\\n    return {\\\"ok\\\": all(g[\\\"ok\\\"] for g in gates), \\\"gates\\\": gates}\\n\\ndef release_candidate_summary():\\n    return {\\\"created_at\\\": now(), \\\"version\\\": \\\"v85.0.0\\\", \\\"ok\\\": True, \\\"readme\\\": str(README_FILE), \\\"report\\\": str(REPORT_FILE)}\\n\\ndef build_release_candidate_report(full=False):\\n    essentials = essential_file_check()\\n    compile_report = compile_core()\\n    gates = gate_simulation() if full else {\\\"ok\\\": True, \\\"skipped\\\": \\\"full gate simulation skipped in summary mode\\\"}\\n    blockers = []\\n    if not essentials[\\\"ok\\\"]:\\n        blockers.append(\\\"missing essential files\\\")\\n    if not compile_report[\\\"ok\\\"]:\\n        blockers.append(\\\"compile failures\\\")\\n    if not gates[\\\"ok\\\"]:\\n        blockers.append(\\\"lower gate failures\\\")\\n    data = {\\n        \\\"created_at\\\": now(),\\n        \\\"version\\\": \\\"v85.0.0\\\",\\n        \\\"ok\\\": len(blockers) == 0,\\n        \\\"blockers\\\": blockers,\\n        \\\"essential_files\\\": essentials,\\n        \\\"compile\\\": {\\\"ok\\\": compile_report[\\\"ok\\\"], \\\"count\\\": compile_report[\\\"count\\\"], \\\"failed\\\": compile_report[\\\"failed\\\"]},\\n        \\\"gates\\\": gates,\\n        \\\"remaining_to_v1\\\": [\\n            \\\"Fix any v85 blockers.\\\",\\n            \\\"Run one-command runtime end-to-end.\\\",\\n            \\\"Create backup and memory export.\\\",\\n            \\\"Test voice/panel/memory/executor/aider once.\\\",\\n            \\\"Write final README and known issues.\\\",\\n        ],\\n    }\\n    REPORT_FILE.write_text(json.dumps(data, indent=4, ensure_ascii=False))\\n    return data\\n\\ndef write_readme():\\n    text = f\\\"\\\"\\\"# Seed v1 Release Candidate\\n\\nGenerated: {now()}\\n\\n## Current layer\\n\\nSeed v85.0.0 \\u2014 Release Candidate preparation stack.\\n\\n## Run\\n\\n```bash\\n./seed\\n```\\n\\nFallback:\\n\\n```bash\\npython seed_runtime_v83.py start\\n```\\n\\n## Main checks\\n\\n```bash\\npython seed_v85_gate.py\\npython seed_v81_gate.py\\npython seed_v75_gate.py\\n```\\n\\n## Main features\\n\\n- Local companion chat\\n- Local Ollama model router\\n- Voice recording/transcription/reply\\n- Avatar/panel interface\\n- Memory review and accepted memory store\\n- Proactive presence inbox\\n- Permissioned executor\\n- Aider coding task loop\\n- Backup/export/forget tools\\n- Recovery and compile checks\\n\\n## Backup\\n\\n```bash\\npython seed_privacy_backup_v84.py\\n```\\n\\nNatural command inside Seed:\\n\\n```text\\nbackup seed\\nexport memory\\nforget memory <keyword>\\n```\\n\\n## Known remaining path\\n\\n- v85 blockers cleanup\\n- v1.0 final stabilization\\n\\\"\\\"\\\"\\n    README_FILE.write_text(text)\\n    return {\\\"ok\\\": True, \\\"file\\\": str(README_FILE)}\\n\\ndef show_release_candidate(full=False):\\n    print(\\\"\\\\n=== SEED v85 RELEASE CANDIDATE ===\\\")\\n    report = build_release_candidate_report(full=full)\\n    readme = write_readme()\\n    print(f\\\"OK: {report['ok']}\\\")\\n    print(f\\\"Blockers: {report['blockers']}\\\")\\n    print(f\\\"Compile: {report['compile']['ok']} ({report['compile']['count']} files)\\\")\\n    print(f\\\"README: {readme['file']}\\\")\\n\\nif __name__ == \\\"__main__\\\":\\n    show_release_candidate(full=\\\"--full\\\" in sys.argv)\\n\", \"seed_v85_systems.py\": \"\\nimport json\\nfrom datetime import datetime\\nfrom pathlib import Path\\n\\nSTATE_FILE = Path(\\\"seed_v85_systems_state.json\\\")\\n\\ndef now():\\n    return datetime.now().isoformat(timespec=\\\"seconds\\\")\\n\\ndef safe(title, summary, fn):\\n    try:\\n        data = fn()\\n        return {\\\"title\\\": title, \\\"summary\\\": summary, \\\"status\\\": \\\"ok\\\" if data.get(\\\"ok\\\", True) else \\\"warning\\\", \\\"data\\\": data}\\n    except Exception as e:\\n        return {\\\"title\\\": title, \\\"summary\\\": summary, \\\"status\\\": \\\"error\\\", \\\"error\\\": str(e)}\\n\\ndef build_v85_state():\\n    cards = [\\n        safe(\\\"v82 Recovery\\\", \\\"Compile checks, recursion guard, dependencies, rollback notes.\\\", lambda: __import__(\\\"seed_recovery_v82\\\", fromlist=[\\\"recovery_summary\\\"]).recovery_summary()),\\n        safe(\\\"v83 One-Command Runtime\\\", \\\"Seed can start via ./seed or seed_start.sh.\\\", lambda: __import__(\\\"seed_runtime_v83\\\", fromlist=[\\\"runtime_status\\\"]).runtime_status()),\\n        safe(\\\"v84 Backup/Privacy\\\", \\\"Backup, export memory, forget by keyword.\\\", lambda: __import__(\\\"seed_privacy_backup_v84\\\", fromlist=[\\\"privacy_status\\\"]).privacy_status()),\\n        safe(\\\"v85 Release Candidate\\\", \\\"RC report and README generation.\\\", lambda: __import__(\\\"seed_release_candidate_v85\\\", fromlist=[\\\"release_candidate_summary\\\"]).release_candidate_summary()),\\n        safe(\\\"Self-State Truth\\\", \\\"Current version override is v85.0.0.\\\", lambda: __import__(\\\"seed_self_state_v85\\\", fromlist=[\\\"build_self_state\\\"]).build_self_state()),\\n    ]\\n    data = {\\\"created_at\\\": now(), \\\"version\\\": \\\"v85.0.0\\\", \\\"ok\\\": all(c[\\\"status\\\"] != \\\"error\\\" for c in cards), \\\"cards\\\": cards}\\n    STATE_FILE.write_text(json.dumps(data, indent=4, ensure_ascii=False))\\n    return data\\n\\ndef show_v85_status():\\n    data = build_v85_state()\\n    print(\\\"\\\\n=== SEED v85 REAL V1 PREP STATUS ===\\\")\\n    print(f\\\"OK: {data['ok']}\\\")\\n    for c in data[\\\"cards\\\"]:\\n        print(f\\\"- {c['title']}: {c['status']} \\u2014 {c['summary']}\\\")\\n\\nif __name__ == \\\"__main__\\\":\\n    show_v85_status()\\n\", \"seed_v85_gate.py\": \"\\nimport json\\nimport subprocess\\nimport sys\\nfrom datetime import datetime\\nfrom pathlib import Path\\n\\nMODULES = [\\n    \\\"seed_self_state_v85.py\\\",\\n    \\\"seed_recovery_v82.py\\\",\\n    \\\"seed_runtime_v83.py\\\",\\n    \\\"seed_privacy_backup_v84.py\\\",\\n    \\\"seed_release_candidate_v85.py\\\",\\n    \\\"seed_v85_systems.py\\\",\\n    \\\"seed_v85_gate.py\\\",\\n    \\\"seed_v85_commands.py\\\",\\n    \\\"seed_natural_intent_router_v85.py\\\",\\n]\\n\\ndef now():\\n    return datetime.now().isoformat(timespec=\\\"seconds\\\")\\n\\ndef compile_module(module):\\n    proc = subprocess.run([sys.executable, \\\"-m\\\", \\\"py_compile\\\", module], capture_output=True, text=True, timeout=30)\\n    return {\\\"module\\\": module, \\\"ok\\\": proc.returncode == 0, \\\"stderr\\\": proc.stderr[-1600:]}\\n\\ndef run_lower_gate(path, timeout=180):\\n    if not Path(path).exists():\\n        return {\\\"ok\\\": False, \\\"error\\\": f\\\"{path} missing\\\"}\\n    try:\\n        proc = subprocess.run([sys.executable, path], capture_output=True, text=True, timeout=timeout)\\n        return {\\\"ok\\\": proc.returncode == 0, \\\"stdout_tail\\\": proc.stdout[-4000:], \\\"stderr_tail\\\": proc.stderr[-4000:]}\\n    except subprocess.TimeoutExpired:\\n        return {\\\"ok\\\": False, \\\"error\\\": f\\\"{path} timeout\\\"}\\n    except Exception as e:\\n        return {\\\"ok\\\": False, \\\"error\\\": str(e)}\\n\\ndef run_v85_gate():\\n    checks = [compile_module(m) for m in MODULES]\\n    modules_ok = all(c[\\\"ok\\\"] for c in checks)\\n    details = {}\\n\\n    try:\\n        from seed_v85_systems import build_v85_state\\n        state = build_v85_state()\\n        systems_ok = state.get(\\\"ok\\\") is True and len(state.get(\\\"cards\\\", [])) >= 5\\n        details[\\\"v85_state\\\"] = {\\\"ok\\\": state.get(\\\"ok\\\"), \\\"cards\\\": len(state.get(\\\"cards\\\", []))}\\n    except Exception as e:\\n        systems_ok = False\\n        details[\\\"v85_state_error\\\"] = str(e)\\n\\n    v81 = run_lower_gate(\\\"seed_v81_gate.py\\\")\\n    v81_ok = v81.get(\\\"ok\\\") is True\\n    details[\\\"v81\\\"] = {\\\"ok\\\": v81_ok, \\\"error\\\": v81.get(\\\"error\\\")}\\n\\n    try:\\n        from seed_self_state_v85 import build_self_state\\n        s = build_self_state()\\n        truth_ok = s.get(\\\"true_current_version\\\") == \\\"v85.0.0\\\"\\n        details[\\\"truth\\\"] = {\\\"current\\\": s.get(\\\"true_current_version\\\"), \\\"green_layers\\\": s.get(\\\"installed_layers_green\\\", [])}\\n    except Exception as e:\\n        truth_ok = False\\n        details[\\\"truth_error\\\"] = str(e)\\n\\n    try:\\n        from seed_runtime_v83 import install_seed_script\\n        script = install_seed_script()\\n        script_ok = script.get(\\\"ok\\\") is True and Path(\\\"seed\\\").exists()\\n        details[\\\"runtime_script\\\"] = script\\n    except Exception as e:\\n        script_ok = False\\n        details[\\\"runtime_script_error\\\"] = str(e)\\n\\n    report = {\\n        \\\"created_at\\\": now(),\\n        \\\"version\\\": \\\"v85.0.0\\\",\\n        \\\"ready\\\": modules_ok and systems_ok and v81_ok and truth_ok and script_ok,\\n        \\\"modules_ok\\\": modules_ok,\\n        \\\"systems_ok\\\": systems_ok,\\n        \\\"v81_ok\\\": v81_ok,\\n        \\\"truth_ok\\\": truth_ok,\\n        \\\"script_ok\\\": script_ok,\\n        \\\"module_checks\\\": checks,\\n        \\\"details\\\": details,\\n    }\\n    open(\\\"seed_v85_gate_report.json\\\", \\\"w\\\").write(json.dumps(report, indent=4))\\n    return report\\n\\ndef show_v85_gate():\\n    r = run_v85_gate()\\n    print(\\\"\\\\n=== SEED v85 REAL V1 PREP GATE ===\\\")\\n    print(f\\\"Ready: {r['ready']}\\\")\\n    print(f\\\"Modules OK: {r['modules_ok']}\\\")\\n    print(f\\\"Systems OK: {r['systems_ok']}\\\")\\n    print(f\\\"v81 OK: {r['v81_ok']}\\\")\\n    print(f\\\"Truth OK: {r['truth_ok']}\\\")\\n    print(f\\\"Script OK: {r['script_ok']}\\\")\\n    print(f\\\"Details: {r['details']}\\\")\\n\\nif __name__ == \\\"__main__\\\":\\n    show_v85_gate()\\n\", \"seed_v85_commands.py\": \"\\ndef handle_v85_command(command):\\n    text = str(command or \\\"\\\").strip()\\n    parts = text.split()\\n    cmd = parts[0].lower() if parts else \\\"\\\"\\n\\n    mapping = {\\n        \\\"/v85-check\\\": (\\\"seed_v85_gate\\\", \\\"show_v85_gate\\\"),\\n        \\\"/v85-status\\\": (\\\"seed_v85_systems\\\", \\\"show_v85_status\\\"),\\n        \\\"/v85-self\\\": (\\\"seed_self_state_v85\\\", \\\"show_self_state\\\"),\\n        \\\"/recovery\\\": (\\\"seed_recovery_v82\\\", \\\"show_recovery\\\"),\\n        \\\"/runtime\\\": (\\\"seed_runtime_v83\\\", \\\"show_runtime\\\"),\\n        \\\"/privacy\\\": (\\\"seed_privacy_backup_v84\\\", \\\"show_privacy\\\"),\\n        \\\"/release-candidate\\\": (\\\"seed_release_candidate_v85\\\", \\\"show_release_candidate\\\"),\\n    }\\n\\n    if cmd == \\\"/v85-help\\\":\\n        print(\\\"\\\"\\\"\\n=== SEED v85 COMMANDS ===\\nNatural:\\n- v85 status\\n- v85 self state\\n- recovery check\\n- mark green checkpoint\\n- recovery notes\\n- runtime status\\n- seed start\\n- stop seed runtime\\n- backup seed\\n- list backups\\n- export memory\\n- forget memory <keyword>\\n- privacy status\\n- release candidate\\n- full release candidate\\n\\nDebug:\\n/v85-check /v85-status /v85-self /recovery /runtime /privacy /release-candidate\\n\\\"\\\"\\\")\\n        return \\\"handled\\\"\\n\\n    if cmd in mapping:\\n        m, f = mapping[cmd]\\n        mod = __import__(m, fromlist=[f])\\n        getattr(mod, f)()\\n        return \\\"handled\\\"\\n\\n    return None\\n\", \"seed_natural_intent_router_v85.py\": \"\\nimport re\\n\\ndef norm(text):\\n    return \\\" \\\".join(str(text or \\\"\\\").strip().lower().split())\\n\\ndef handle_natural_intent_v85(user_message):\\n    raw = str(user_message or \\\"\\\").strip()\\n    text = norm(raw)\\n    if not text or raw.startswith(\\\"/\\\"):\\n        return None\\n\\n    if any(p in text for p in [\\\"v85 status\\\", \\\"real v1 prep status\\\", \\\"v1 prep status\\\"]):\\n        from seed_v85_systems import show_v85_status\\n        show_v85_status()\\n        return \\\"handled\\\"\\n\\n    if any(p in text for p in [\\\"v85 self state\\\", \\\"real v1 state\\\", \\\"what version are you\\\"]):\\n        from seed_self_state_v85 import show_self_state\\n        show_self_state()\\n        return \\\"handled\\\"\\n\\n    if any(p in text for p in [\\\"recovery check\\\", \\\"self repair check\\\", \\\"health check\\\"]):\\n        from seed_recovery_v82 import show_recovery\\n        show_recovery()\\n        return \\\"handled\\\"\\n\\n    if \\\"mark green checkpoint\\\" in text:\\n        from seed_recovery_v82 import mark_last_green\\n        print(\\\"\\\\n=== SEED v82 GREEN CHECKPOINT ===\\\")\\n        print(mark_last_green(\\\"natural_command_green_checkpoint\\\"))\\n        return \\\"handled\\\"\\n\\n    if \\\"recovery notes\\\" in text:\\n        from seed_recovery_v82 import write_rollback_notes\\n        print(\\\"\\\\n=== SEED v82 RECOVERY NOTES ===\\\")\\n        print(write_rollback_notes())\\n        return \\\"handled\\\"\\n\\n    if any(p in text for p in [\\\"runtime status\\\", \\\"one command runtime\\\"]):\\n        from seed_runtime_v83 import show_runtime\\n        show_runtime()\\n        return \\\"handled\\\"\\n\\n    if any(p in text for p in [\\\"seed start\\\", \\\"start seed runtime\\\", \\\"start runtime\\\"]):\\n        from seed_runtime_v83 import seed_start\\n        seed_start(run_gate=False)\\n        return \\\"handled\\\"\\n\\n    if any(p in text for p in [\\\"stop seed runtime\\\", \\\"stop runtime\\\"]):\\n        from seed_runtime_v83 import stop_panel\\n        print(stop_panel())\\n        return \\\"handled\\\"\\n\\n    if any(p in text for p in [\\\"backup seed\\\", \\\"create backup\\\", \\\"backup project\\\"]):\\n        from seed_privacy_backup_v84 import backup_project\\n        print(\\\"\\\\n=== SEED v84 BACKUP ===\\\")\\n        print(backup_project(\\\"natural\\\"))\\n        return \\\"handled\\\"\\n\\n    if any(p in text for p in [\\\"list backups\\\", \\\"show backups\\\"]):\\n        from seed_privacy_backup_v84 import list_backups\\n        import json\\n        print(\\\"\\\\n=== SEED v84 BACKUPS ===\\\")\\n        print(json.dumps(list_backups(), indent=4, ensure_ascii=False))\\n        return \\\"handled\\\"\\n\\n    if any(p in text for p in [\\\"export memory\\\", \\\"memory export\\\"]):\\n        from seed_privacy_backup_v84 import export_memory\\n        print(\\\"\\\\n=== SEED v84 MEMORY EXPORT ===\\\")\\n        print(export_memory())\\n        return \\\"handled\\\"\\n\\n    m = re.search(r\\\"\\\\bforget memory\\\\s+(.+)$\\\", raw, flags=re.I)\\n    if m:\\n        from seed_privacy_backup_v84 import forget_keyword\\n        print(\\\"\\\\n=== SEED v84 FORGET MEMORY ===\\\")\\n        print(forget_keyword(m.group(1).strip()))\\n        return \\\"handled\\\"\\n\\n    if any(p in text for p in [\\\"privacy status\\\", \\\"backup status\\\"]):\\n        from seed_privacy_backup_v84 import show_privacy\\n        show_privacy()\\n        return \\\"handled\\\"\\n\\n    if any(p in text for p in [\\\"release candidate\\\", \\\"rc report\\\"]):\\n        from seed_release_candidate_v85 import show_release_candidate\\n        show_release_candidate(full=False)\\n        return \\\"handled\\\"\\n\\n    if any(p in text for p in [\\\"full release candidate\\\", \\\"full rc report\\\", \\\"fresh install simulation\\\"]):\\n        from seed_release_candidate_v85 import show_release_candidate\\n        show_release_candidate(full=True)\\n        return \\\"handled\\\"\\n\\n    return None\\n\"}")

def write(path, text):
    Path(path).write_text(text.strip() + "\n")
    print("Wrote", path)

for path, text in MODULES.items():
    write(path, text)

# Patch seed_commands.py with v85 router.
p = Path("seed_commands.py")
text = p.read_text(errors="ignore") if p.exists() else "def handle_chat_command(user_message,*args,**kwargs): return None\n"
if "_seed_v85_previous_handle_chat_command" not in text:
    text += """
# v85 Real-v1 prep router.
try:
    _seed_v85_previous_handle_chat_command = handle_chat_command

    def handle_chat_command(user_message, *args, **kwargs):
        try:
            from seed_natural_intent_router_v85 import handle_natural_intent_v85
            handled = handle_natural_intent_v85(user_message)
            if handled == "handled":
                return "handled"
        except Exception as error:
            print(f"v85 natural router error: {error}")
            return "handled"

        try:
            from seed_v85_commands import handle_v85_command
            handled = handle_v85_command(user_message)
            if handled == "handled":
                return "handled"
        except Exception as error:
            print(f"v85 command error: {error}")
            return "handled"

        return _seed_v85_previous_handle_chat_command(user_message, *args, **kwargs)

except Exception:
    pass
"""
    p.write_text(text)
    print("Patched seed_commands.py")

# Patch chat truth context.
p = Path("seed_local_chat_v701.py")
if p.exists():
    text = p.read_text(errors="ignore")
    if "v85 Self-State Truth chat override" not in text:
        text += """
# v85 Self-State Truth chat override.
try:
    _seed_v85_old_build_seed_context = build_seed_context

    def build_seed_context():
        base = _seed_v85_old_build_seed_context()
        try:
            from seed_self_state_v85 import build_seed_truth_context
            truth = build_seed_truth_context()
        except Exception as error:
            truth = f"Seed v85 truth context unavailable: {error}"
        return base + "\\n\\n" + truth

except Exception:
    pass

try:
    _seed_v85_old_prompt_for = prompt_for

    def prompt_for(role, user_message):
        seed_context = build_seed_context()
        base = f\"\"\"You are Seed, User's local AI companion running on his Mac.

{seed_context}

Current user message:
User: {user_message}

Answer as Seed.
Do not answer with placeholder words like "normal".
Do not invent unrelated suggestions.
When asked about your current version/state, use the TRUE CURRENT SEED STATE OVERRIDE.
Current version is v85.0.0 if the v85 gate is green.
v70/v75/v81 are older green layers, not the current version.
You may be expressive/playful and use simulated emotion honestly.
You may give relevant life advice when grounded in User goals, memory, project, health, school, work, or current context.
\"\"\"
        if role == "seed_status":
            base += \"\"\"
For this answer, focus on current truth:
- current layer is v85.0.0
- v82 recovery/self-repair is installed
- v83 one-command runtime is installed
- v84 backup/privacy/export/forget is installed
- v85 release candidate checks are installed
- next real-v1 path: fix RC blockers, then Seed v1.0 final release
\"\"\"
        if role == "coding":
            base += "\\nFor coding or patch tasks, be concrete: files, commands, tests, rollback.\\n"
        if role == "turkish":
            base += "\\nRespond naturally in Turkish unless English is clearly better.\\n"
        return base + "\\nSeed:"

except Exception:
    pass
"""
        p.write_text(text)
        print("Patched seed_local_chat_v701.py")

# Patch config.
p = Path("seed_config.py")
text = p.read_text(errors="ignore") if p.exists() else 'SEED_VERSION = "v85.0.0"\n'
text = re.sub(r'^SEED_VERSION\s*=\s*".*?"', 'SEED_VERSION = "v85.0.0"', text, flags=re.M)
if "SEED_V85_REAL_V1_PREP" not in text:
    text += """
SEED_V82_RECOVERY = True
SEED_V83_ONE_COMMAND_RUNTIME = True
SEED_V84_BACKUP_PRIVACY = True
SEED_V85_RELEASE_CANDIDATE = True
SEED_V85_REAL_V1_PREP = True
"""
p.write_text(text)
print("Updated seed_config.py")

# Patch control plane API.
p = Path("seed_control_plane_server.py")
if p.exists():
    text = p.read_text(errors="ignore")
    if '/api/v85' not in text:
        endpoint = '    if path == "/api/v85":\n        return safe_json(lambda: __import__("seed_v85_systems", fromlist=["build_v85_state"]).build_v85_state())\n\n'
        if '    if path == "/api/v81":\n' in text:
            text = text.replace('    if path == "/api/v81":\n', endpoint + '    if path == "/api/v81":\n', 1)
        elif '    if path == "/api/v75":\n' in text:
            text = text.replace('    if path == "/api/v75":\n', endpoint + '    if path == "/api/v75":\n', 1)
    if '"v85": api_payload("/api/v85")' not in text and '"v81": api_payload("/api/v81")' in text:
        text = text.replace('"v81": api_payload("/api/v81")', '"v85": api_payload("/api/v85"),\n        "v81": api_payload("/api/v81")', 1)
    p.write_text(text)
    print("Patched seed_control_plane_server.py")

# Patch gate runners.
for filename, list_name in [("seed_final_gate_runner.py", "FINAL_GATE_COMMANDS"), ("seed_quick_gate_runner.py", "QUICK_GATE_COMMANDS")]:
    p = Path(filename)
    if p.exists():
        text = p.read_text(errors="ignore")
        line = '    ["python", "seed_v85_gate.py"],\n'
        if line not in text and f"{list_name} = [" in text:
            text = text.replace(f"{list_name} = [\n", f"{list_name} = [\n" + line, 1)
            p.write_text(text)
            print("Patched", filename)

# Docs.
p = Path("Seed_Core.md")
text = p.read_text(errors="ignore") if p.exists() else ""
if "Seed v85.0.0 — Real v1 Preparation Stack" not in text:
    text += """
## Seed v85.0.0 — Real v1 Preparation Stack

Combined v82-v85 update:
- v82 Recovery / self-repair / rollback notes
- v83 One-command runtime
- v84 Backup / privacy / export / forget
- v85 Release candidate checks and README

Main commands:
- v85 status
- v85 self state
- recovery check
- mark green checkpoint
- recovery notes
- runtime status
- seed start
- stop seed runtime
- backup seed
- list backups
- export memory
- forget memory <keyword>
- privacy status
- release candidate
- full release candidate

Shell:
- ./seed
- ./seed_start.sh
"""
p.write_text(text)
print("Updated Seed_Core.md")

# Gitignore.
p = Path(".gitignore")
text = p.read_text(errors="ignore") if p.exists() else ""
block = """
# Seed v82-v85 runtime
seed_self_state_v85.json
seed_recovery_report_v82.json
seed_last_green_checkpoint_v82.json
seed_recovery_rollback_notes_v82.md
seed_runtime_v83_state.json
seed_runtime_v83_panel.log
seed_runtime_v83_panel.pid
seed_backups_v84/
seed_exports_v84/
seed_forget_log_v84.jsonl
seed_v85_systems_state.json
seed_v85_gate_report.json
seed_v85_release_candidate_report.json
README_SEED_V1_RC.md
"""
if "Seed v82-v85 runtime" not in text:
    text += "\n" + block
p.write_text(text)
print("Updated .gitignore")

# Install one-command script now.
try:
    from seed_runtime_v83 import install_seed_script
    print("Installed runtime script:", install_seed_script())
except Exception as error:
    print("Runtime script install skipped:", error)

# Compile checks.
checks = list(MODULES.keys()) + ["seed_commands.py", "seed_local_chat_v701.py"]
for m in checks:
    if Path(m).exists():
        proc = subprocess.run([sys.executable, "-m", "py_compile", m], capture_output=True, text=True, timeout=30)
        print("$ python -m py_compile", m)
        if proc.returncode == 0:
            print("OK")
        else:
            print(proc.stderr)
            sys.exit(proc.returncode)

print("\nSeed v85 Real-v1 prep installer complete.")
