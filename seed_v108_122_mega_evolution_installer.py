#!/usr/bin/env python3
from pathlib import Path
import subprocess, sys, re, json

FILES = {"seed_proactive_rhythm_v108.py": "import json, os, signal, subprocess, sys, time\nfrom datetime import datetime, timedelta\nfrom pathlib import Path\nSETTINGS=Path(\"seed_proactive_v108_settings.json\"); EVENTS=Path(\"seed_proactive_v108_events.jsonl\"); PID=Path(\"seed_proactive_v108.pid\"); LOG=Path(\"seed_proactive_v108.log\")\nDEFAULT={\"version\":\"v108.0.0\",\"enabled\":True,\"speak_enabled\":False,\"max_asks_per_day\":3,\"cooldown_minutes\":90,\"quiet_hours\":{\"start\":1,\"end\":10},\"tick_seconds\":900}\nPROMPTS=[\"Kanka, küçük kontrol: Seed tarafında şimdi memory, wake, dashboard, yoksa operator tarafını mı güçlendirelim?\",\"Ben buradayım. Bugün Seed için bir sonraki küçük hedefi seçelim mi?\",\"Açık task varsa toparlayayım mı, yoksa sessizce izlemeye devam mı?\"]\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef settings():\n    if SETTINGS.exists():\n        try:\n            d=DEFAULT.copy(); d.update(json.loads(SETTINGS.read_text(errors=\"ignore\"))); d[\"version\"]=\"v108.0.0\"; return d\n        except Exception: pass\n    SETTINGS.write_text(json.dumps(DEFAULT,indent=4,ensure_ascii=False)); return DEFAULT.copy()\ndef save(d): d[\"version\"]=\"v108.0.0\"; SETTINGS.write_text(json.dumps(d,indent=4,ensure_ascii=False)); return d\ndef log(row):\n    row.setdefault(\"created_at\",now()); row.setdefault(\"version\",\"v108.0.0\")\n    with EVENTS.open(\"a\") as f: f.write(json.dumps(row,ensure_ascii=False)+\"\\n\")\ndef events(limit=5000):\n    if not EVENTS.exists(): return []\n    out=[]\n    for l in EVENTS.read_text(errors=\"ignore\").splitlines()[-limit:]:\n        try: out.append(json.loads(l))\n        except Exception: pass\n    return out\ndef quiet():\n    q=settings().get(\"quiet_hours\",{}); h=datetime.now().hour; a=int(q.get(\"start\",1)); b=int(q.get(\"end\",10)); return a<=h<b if a<b else h>=a or h<b\ndef asks_today():\n    today=datetime.now().date().isoformat(); return [e for e in events() if e.get(\"event\")==\"ask\" and str(e.get(\"created_at\",\"\")).startswith(today)]\ndef last_ask():\n    xs=[e for e in events() if e.get(\"event\")==\"ask\"]\n    if not xs: return None\n    try: return datetime.fromisoformat(xs[-1][\"created_at\"])\n    except Exception: return None\ndef task_signal():\n    try:\n        import seed_tasks_v99 as t; st=t.status(); op=st.get(\"open\",st.get(\"tasks\",[])); return {\"ok\":True,\"open_count\":len(op),\"open\":op[:3]}\n    except Exception as e: return {\"ok\":False,\"open_count\":0,\"error\":str(e)}\ndef should_ask():\n    s=settings()\n    if not s.get(\"enabled\",True): return {\"ok\":True,\"should_ask\":False,\"reason\":\"disabled\"}\n    if quiet(): return {\"ok\":True,\"should_ask\":False,\"reason\":\"quiet_hours\"}\n    if len(asks_today())>=int(s.get(\"max_asks_per_day\",3)): return {\"ok\":True,\"should_ask\":False,\"reason\":\"max_asks_per_day\"}\n    la=last_ask()\n    if la and datetime.now()-la < timedelta(minutes=int(s.get(\"cooldown_minutes\",90))): return {\"ok\":True,\"should_ask\":False,\"reason\":\"cooldown\"}\n    ts=task_signal(); reasons=[]\n    if ts.get(\"open_count\",0)>0: reasons.append(\"open_tasks\")\n    if not reasons: reasons.append(\"gentle_checkin\")\n    return {\"ok\":True,\"should_ask\":True,\"reason\":reasons[0],\"reasons\":reasons,\"signals\":{\"tasks\":ts}}\ndef question(reason):\n    return \"Kanka, açık task var gibi. Bunu kapatalım mı, yoksa Seed bir sonraki planı mı çıkarsın?\" if reason==\"open_tasks\" else PROMPTS[len(asks_today())%len(PROMPTS)]\ndef tick(speak=False):\n    d=should_ask()\n    if not d.get(\"should_ask\"):\n        log({\"event\":\"tick\",\"asked\":False,\"decision\":d}); return {\"ok\":True,\"asked\":False,\"decision\":d}\n    q=question(d.get(\"reason\")); row={\"event\":\"ask\",\"asked\":True,\"question\":q,\"decision\":d,\"speak\":bool(speak or settings().get(\"speak_enabled\",False))}\n    if row[\"speak\"]:\n        try:\n            import seed_tts_v111 as tts; row[\"tts\"]=tts.say(q)\n        except Exception as e: row[\"tts\"]={\"ok\":False,\"error\":str(e)}\n    log(row); return {\"ok\":True,**row}\ndef alive(pid):\n    try: os.kill(int(pid),0); return True\n    except Exception: return False\ndef daemon():\n    log({\"event\":\"daemon_started\"})\n    while True:\n        try: tick()\n        except Exception as e: log({\"event\":\"daemon_error\",\"error\":str(e)})\n        time.sleep(int(settings().get(\"tick_seconds\",900)))\ndef start(speak=False):\n    s=settings(); s[\"enabled\"]=True\n    if speak: s[\"speak_enabled\"]=True\n    save(s)\n    if PID.exists():\n        try:\n            pid=int(PID.read_text().strip())\n            if alive(pid): return {\"ok\":True,\"already_running\":True,\"pid\":pid}\n        except Exception: pass\n    p=subprocess.Popen([sys.executable,\"seed_proactive_rhythm_v108.py\",\"daemon\"],stdout=LOG.open(\"a\"),stderr=LOG.open(\"a\")); PID.write_text(str(p.pid)); return {\"ok\":True,\"pid\":p.pid}\ndef stop():\n    pid=None; stopped=False\n    if PID.exists():\n        try:\n            pid=int(PID.read_text().strip())\n            if alive(pid): os.kill(pid,signal.SIGTERM); stopped=True\n            PID.unlink(missing_ok=True)\n        except Exception: pass\n    return {\"ok\":True,\"stopped\":stopped,\"pid\":pid}\ndef test():\n    old=settings(); tmp=old.copy(); tmp.update({\"enabled\":True,\"max_asks_per_day\":99,\"cooldown_minutes\":0,\"quiet_hours\":{\"start\":25,\"end\":26}}); save(tmp); d=should_ask(); save(old); return {\"ok\":d.get(\"should_ask\") in {True,False},\"decision\":d}\ndef status():\n    pid=None; al=False\n    if PID.exists():\n        try: pid=int(PID.read_text().strip()); al=alive(pid)\n        except Exception: pass\n    return {\"created_at\":now(),\"version\":\"v108.0.0\",\"ok\":True,\"alive\":al,\"pid\":pid,\"settings\":settings(),\"today_asks\":len(asks_today()),\"decision_now\":should_ask(),\"last_events\":events(10)}\nif __name__==\"__main__\":\n    import sys, json\n    a=sys.argv[1] if len(sys.argv)>1 else \"status\"\n    print(json.dumps(start(\"--speak\" in sys.argv) if a==\"start\" else stop() if a==\"stop\" else tick(\"--speak\" in sys.argv) if a==\"tick\" else test() if a==\"test\" else status(),indent=4,ensure_ascii=False))\n", "seed_native_wake_v109.py": "import json, importlib.util, shutil, sys\nfrom datetime import datetime\nfrom pathlib import Path\nSETTINGS=Path(\"seed_native_wake_v109_settings.json\")\nDEFAULT={\"version\":\"v109.0.0\",\"preferred\":\"openwakeword\",\"fallback\":\"v107_matcher\",\"store_raw_audio\":False,\"custom_model_path\":\"\"}\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef settings():\n    if SETTINGS.exists():\n        try:\n            d=DEFAULT.copy(); d.update(json.loads(SETTINGS.read_text(errors=\"ignore\"))); d[\"version\"]=\"v109.0.0\"; return d\n        except Exception: pass\n    SETTINGS.write_text(json.dumps(DEFAULT,indent=4)); return DEFAULT.copy()\ndef detect(): return {\"openwakeword\":importlib.util.find_spec(\"openwakeword\") is not None,\"porcupine\":importlib.util.find_spec(\"pvporcupine\") is not None,\"fallback_v107\":Path(\"seed_wake_reliability_v107.py\").exists()}\ndef route():\n    d=detect()\n    if d[\"openwakeword\"]: return \"openwakeword\"\n    if d[\"porcupine\"]: return \"porcupine\"\n    return \"v107_matcher\" if d[\"fallback_v107\"] else \"none\"\ndef match(text):\n    try:\n        from seed_wake_reliability_v107 import match_wake_reliable\n        return {\"ok\":True,\"engine\":route(),\"match\":match_wake_reliable(text)}\n    except Exception as e: return {\"ok\":False,\"engine\":route(),\"error\":str(e)}\ndef test():\n    cases={\"make up status\":True,\"wake app status\":True,\"pumpkin seed recipe\":False,\"hello there\":False}; res={}; ok=True\n    for t,e in cases.items():\n        g=match(t).get(\"match\",(False,None,\"\"))[0]; res[t]={\"expected\":e,\"got\":g}; ok=ok and (g==e)\n    return {\"created_at\":now(),\"version\":\"v109.0.0\",\"ok\":ok,\"route\":route(),\"detect\":detect(),\"results\":res}\ndef status(): return {\"created_at\":now(),\"version\":\"v109.0.0\",\"ok\":True,\"route\":route(),\"detect\":detect(),\"settings\":settings()}\nif __name__==\"__main__\":\n    a=sys.argv[1] if len(sys.argv)>1 else \"status\"\n    print(json.dumps(match(\" \".join(sys.argv[2:])) if a==\"match\" else test() if a==\"test\" else status(),indent=4,ensure_ascii=False))\n", "seed_stt_v110.py": "import json, shutil, subprocess, sys\nfrom datetime import datetime\nfrom pathlib import Path\nSETTINGS=Path(\"seed_stt_v110_settings.json\")\nDEFAULT={\"version\":\"v110.0.0\",\"engine\":\"auto\",\"whisper_cpp_binary\":\"\",\"model_path\":\"\",\"language\":\"auto\",\"store_raw_audio\":False,\"timeout_seconds\":120}\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef settings():\n    if SETTINGS.exists():\n        try:\n            d=DEFAULT.copy(); d.update(json.loads(SETTINGS.read_text(errors=\"ignore\"))); d[\"version\"]=\"v110.0.0\"; return d\n        except Exception: pass\n    SETTINGS.write_text(json.dumps(DEFAULT,indent=4)); return DEFAULT.copy()\ndef has(m):\n    import importlib.util; return importlib.util.find_spec(m) is not None\ndef detect(): return {\"whisper_cli\":shutil.which(\"whisper-cli\"),\"whisper_main\":shutil.which(\"main\"),\"ffmpeg\":shutil.which(\"ffmpeg\"),\"faster_whisper\":has(\"faster_whisper\"),\"python_whisper\":has(\"whisper\")}\ndef binary():\n    s=settings(); return s.get(\"whisper_cpp_binary\") if s.get(\"whisper_cpp_binary\") and Path(s[\"whisper_cpp_binary\"]).exists() else (shutil.which(\"whisper-cli\") or shutil.which(\"main\"))\ndef transcribe(path):\n    p=Path(path)\n    if not p.exists(): return {\"ok\":False,\"error\":\"audio file not found\"}\n    b=binary(); s=settings()\n    if not b: return {\"ok\":False,\"error\":\"whisper.cpp binary not found\",\"detect\":detect()}\n    if not s.get(\"model_path\") or not Path(s[\"model_path\"]).exists(): return {\"ok\":False,\"error\":\"model_path missing\",\"binary\":b}\n    cmd=[b,\"-m\",s[\"model_path\"],\"-f\",str(p)]\n    if s.get(\"language\")!=\"auto\": cmd+=[\"-l\",s[\"language\"]]\n    pr=subprocess.run(cmd,capture_output=True,text=True,timeout=int(s.get(\"timeout_seconds\",120)))\n    return {\"ok\":pr.returncode==0,\"text\":(pr.stdout+\"\\n\"+pr.stderr)[-6000:],\"cmd\":cmd}\ndef status(): return {\"created_at\":now(),\"version\":\"v110.0.0\",\"ok\":True,\"detect\":detect(),\"chosen_binary\":binary(),\"settings\":settings(),\"ready\":bool(binary() and settings().get(\"model_path\"))}\ndef test(): return {\"created_at\":now(),\"version\":\"v110.0.0\",\"ok\":True,\"status\":status(),\"note\":\"wrapper ready; real transcription requires binary+model_path\"}\nif __name__==\"__main__\":\n    a=sys.argv[1] if len(sys.argv)>1 else \"status\"\n    print(json.dumps(transcribe(sys.argv[2] if len(sys.argv)>2 else \"\") if a==\"transcribe\" else test() if a==\"test\" else status(),indent=4,ensure_ascii=False))\n", "seed_tts_v111.py": "import json, shutil, subprocess, sys\nfrom pathlib import Path\nfrom datetime import datetime\nSETTINGS=Path(\"seed_tts_v111_settings.json\")\nDEFAULT={\"version\":\"v111.0.0\",\"engine\":\"auto\",\"mode\":\"normal\",\"rate\":185,\"piper_binary\":\"\",\"piper_model\":\"\",\"voice\":\"\",\"timeout_seconds\":90}\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef settings():\n    if SETTINGS.exists():\n        try:\n            d=DEFAULT.copy(); d.update(json.loads(SETTINGS.read_text(errors=\"ignore\"))); d[\"version\"]=\"v111.0.0\"; return d\n        except Exception: pass\n    SETTINGS.write_text(json.dumps(DEFAULT,indent=4)); return DEFAULT.copy()\ndef save(d): d[\"version\"]=\"v111.0.0\"; SETTINGS.write_text(json.dumps(d,indent=4)); return d\ndef mode(m):\n    d=settings(); d[\"mode\"]=m; d[\"rate\"]=155 if m==\"night\" else 205 if m==\"excited\" else 165 if m==\"quiet\" else 185; return save(d)\ndef say(text):\n    s=settings(); text=str(text)[:1200]; piper=s.get(\"piper_binary\") or shutil.which(\"piper\")\n    if piper and s.get(\"piper_model\") and s.get(\"engine\") in {\"auto\",\"piper\"}:\n        pr=subprocess.run([piper,\"--model\",s[\"piper_model\"],\"--output_file\",\"/tmp/seed_tts.wav\"],input=text,capture_output=True,text=True,timeout=int(s[\"timeout_seconds\"]))\n        return {\"ok\":pr.returncode==0,\"engine\":\"piper\",\"stderr\":pr.stderr[-1000:]}\n    cmd=[\"say\",\"-r\",str(s.get(\"rate\",185))]\n    if s.get(\"voice\"): cmd+=[\"-v\",s[\"voice\"]]\n    cmd.append(text); pr=subprocess.run(cmd,capture_output=True,text=True,timeout=int(s[\"timeout_seconds\"]))\n    return {\"ok\":pr.returncode==0,\"engine\":\"macos_say\",\"stderr\":pr.stderr[-1000:]}\ndef status(): return {\"created_at\":now(),\"version\":\"v111.0.0\",\"ok\":True,\"detect\":{\"say\":shutil.which(\"say\"),\"piper\":shutil.which(\"piper\")},\"settings\":settings()}\nif __name__==\"__main__\":\n    a=sys.argv[1] if len(sys.argv)>1 else \"status\"\n    print(json.dumps(say(\" \".join(sys.argv[2:])) if a==\"say\" else mode(sys.argv[2] if len(sys.argv)>2 else \"normal\") if a==\"mode\" else status(),indent=4,ensure_ascii=False))\n", "seed_memory_garden3_v112.py": "import json,re,time\nfrom datetime import datetime\nfrom pathlib import Path\nMEM=Path(\"seed_memory_garden3_v112.jsonl\")\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef write(row):\n    row.setdefault(\"memory_id\",f\"m3_{int(time.time()*1000)}\"); row.setdefault(\"created_at\",now()); row.setdefault(\"version\",\"v112.0.0\"); row.setdefault(\"trust\",0.75); row.setdefault(\"forgotten\",False)\n    with MEM.open(\"a\") as f:f.write(json.dumps(row,ensure_ascii=False)+\"\\n\")\n    return row\ndef read():\n    if not MEM.exists(): return []\n    out=[]\n    for l in MEM.read_text(errors=\"ignore\").splitlines():\n        try: out.append(json.loads(l))\n        except Exception: pass\n    return out\ndef add(memory_type,summary,tags=None,source=\"manual\",trust=0.75): return {\"ok\":True,\"memory\":write({\"memory_type\":memory_type,\"summary\":summary,\"tags\":tags or [],\"source\":source,\"trust\":trust})}\ndef import_legacy():\n    existing={m.get(\"summary\") for m in read()}; n=0\n    try:\n        import seed_memory_garden2_v96 as m2\n        for h in m2.search(\"seed milestone\"):\n            m=h.get(\"memory\",{}); s=m.get(\"summary\")\n            if s and s not in existing: write({\"memory_type\":m.get(\"memory_type\",\"milestone\"),\"summary\":s,\"tags\":m.get(\"tags\",[]),\"source\":\"legacy_v96\",\"trust\":0.9}); n+=1\n    except Exception: pass\n    return {\"ok\":True,\"imported\":n}\ndef search(q,limit=20):\n    terms=[x for x in re.sub(r\"[^a-z0-9çğıöşü\\s]\",\" \",q.lower()).split() if len(x)>1]; hits=[]\n    for m in read():\n        if m.get(\"forgotten\"): continue\n        hay=(m.get(\"summary\",\"\")+\" \"+\" \".join(m.get(\"tags\",[]))+\" \"+m.get(\"memory_type\",\"\")).lower(); sc=sum(hay.count(t) for t in terms)+float(m.get(\"trust\",0.5))\n        if sc>0.5: hits.append({\"score\":round(sc,2),\"memory\":m})\n    return sorted(hits,key=lambda x:x[\"score\"],reverse=True)[:int(limit)]\ndef status():\n    import_legacy(); items=read(); by={}\n    for m in items: by[m.get(\"memory_type\",\"unknown\")]=by.get(m.get(\"memory_type\",\"unknown\"),0)+1\n    return {\"created_at\":now(),\"version\":\"v112.0.0\",\"ok\":True,\"count\":len(items),\"by_type\":by,\"latest\":items[-5:]}\nif __name__==\"__main__\":\n    import sys\n    a=sys.argv[1] if len(sys.argv)>1 else \"status\"\n    print(json.dumps(add(sys.argv[2],\" \".join(sys.argv[3:])) if a==\"add\" else search(\" \".join(sys.argv[2:])) if a==\"search\" else import_legacy() if a==\"import\" else status(),indent=4,ensure_ascii=False))\n", "seed_memory_gate_v113.py": "import json,re\nfrom datetime import datetime\nSENSITIVE={'health','medical','religion','politics','address','password','token','api key'}\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef domain(t):\n    s=set(re.sub(r\"[^a-z0-9\\s]\",\" \",str(t).lower()).split())\n    if s & {'seed','memory','wake','dashboard','operator','rag'}: return 'seed'\n    if s & {'pain','doctor','medical','heart'}: return 'health'\n    return 'general'\ndef gate_memory(m,q):\n    md=domain(m.get(\"summary\",\"\")+\" \"+\" \".join(m.get(\"tags\",[]))); qd=domain(q); sens=[x for x in SENSITIVE if x in m.get(\"summary\",\"\").lower()]; allowed=not(sens and md!=qd) and float(m.get(\"trust\",0.5))>=0.35 and not m.get(\"forgotten\")\n    return {\"allowed\":allowed,\"domain\":qd,\"memory_domain\":md,\"sensitive\":sens}\ndef retrieve(q,limit=12):\n    import seed_memory_garden3_v112 as mg\n    out=[]\n    for h in mg.search(q,limit*3):\n        g=gate_memory(h[\"memory\"],q)\n        if g[\"allowed\"]: out.append({\"score\":h[\"score\"],\"gate\":g,\"memory\":h[\"memory\"]})\n        if len(out)>=limit: break\n    return out\ndef test():\n    return {\"ok\":gate_memory({\"summary\":\"Seed dashboard green\",\"trust\":.9},\"seed\")[\"allowed\"] and not gate_memory({\"summary\":\"medical pain note health\",\"trust\":.9},\"seed\")[\"allowed\"]}\ndef status(): return {\"created_at\":now(),\"version\":\"v113.0.0\",\"ok\":True,\"test\":test()}\nif __name__==\"__main__\":\n    import sys; print(json.dumps(retrieve(\" \".join(sys.argv[2:])) if len(sys.argv)>1 and sys.argv[1]==\"retrieve\" else test() if len(sys.argv)>1 and sys.argv[1]==\"test\" else status(),indent=4,ensure_ascii=False))\n", "seed_project_memory_v114.py": "import json,subprocess,time,re\nfrom pathlib import Path\nfrom datetime import datetime\nEVENTS=Path(\"seed_project_memory_v114.jsonl\")\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef write(r):\n    r.setdefault(\"event_id\",f\"pm_{int(time.time()*1000)}\"); r.setdefault(\"created_at\",now()); r.setdefault(\"version\",\"v114.0.0\")\n    with EVENTS.open(\"a\") as f:f.write(json.dumps(r,ensure_ascii=False)+\"\\n\")\n    return r\ndef read():\n    if not EVENTS.exists(): return []\n    out=[]\n    for l in EVENTS.read_text(errors=\"ignore\").splitlines():\n        try: out.append(json.loads(l))\n        except Exception: pass\n    return out\ndef record(kind,summary,outcome='unknown'): return {\"ok\":True,\"event\":write({\"kind\":kind,\"summary\":summary,\"outcome\":outcome})}\ndef gate(action):\n    warns=[e for e in read() if e.get(\"outcome\") in {\"failed\",\"avoid\"} and any(w in action.lower() for w in e.get(\"summary\",\"\").lower().split() if len(w)>4)]\n    return {\"ok\":True,\"allowed\":True,\"warning_count\":len(warns),\"warnings\":warns[:5]}\ndef git_status():\n    try:\n        p=subprocess.run([\"git\",\"status\",\"--short\"],capture_output=True,text=True,timeout=20); return {\"ok\":p.returncode==0,\"stdout\":p.stdout}\n    except Exception as e:return {\"ok\":False,\"error\":str(e)}\ndef status(): return {\"created_at\":now(),\"version\":\"v114.0.0\",\"ok\":True,\"events\":len(read()),\"git\":git_status(),\"latest\":read()[-5:]}\ndef test(): return {\"ok\":gate(\"safe dashboard status\")[\"ok\"]}\nif __name__==\"__main__\":\n    import sys\n    a=sys.argv[1] if len(sys.argv)>1 else \"status\"\n    print(json.dumps(record(a,\" \".join(sys.argv[2:])) if a in {\"fix\",\"decision\",\"mistake\"} else gate(\" \".join(sys.argv[2:])) if a==\"gate\" else test() if a==\"test\" else status(),indent=4,ensure_ascii=False))\n", "seed_operator2_v115.py": "import json, subprocess\nfrom datetime import datetime\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef active_window():\n    try:\n        p=subprocess.run([\"osascript\",\"-e\",'tell application \"System Events\" to get name of first application process whose frontmost is true'],capture_output=True,text=True,timeout=5); return {\"ok\":p.returncode==0,\"app\":p.stdout.strip()}\n    except Exception as e:return {\"ok\":False,\"error\":str(e)}\ndef plan(a,t=''): return {\"ok\":True,\"action\":a,\"target\":t,\"risk\":\"observe\" if a in {\"status\",\"active-window\"} else \"risky\",\"steps\":[\"classify\",\"approve if needed\",\"execute\",\"log\"]}\ndef run(a,t='',approved=False):\n    import seed_safety_ledger_v94 as s; dec=s.decision(\"operator2 \"+a,target=t,approved=approved)\n    if not dec.get(\"allowed\"): return {\"ok\":False,\"blocked\":True,\"plan\":plan(a,t),\"decision\":dec}\n    if a==\"active-window\": res=active_window()\n    else:\n        import seed_operator_v100 as op; res=op.run({\"open-url\":\"open-url\",\"open-app\":\"open-app\",\"speak\":\"speak\",\"type\":\"type\",\"press\":\"press\",\"screenshot\":\"screenshot\"}.get(a,a),t,approved)\n    return {\"created_at\":now(),\"version\":\"v115.0.0\",\"ok\":res.get(\"ok\",False),\"decision\":dec,\"result\":res}\ndef status(): return {\"created_at\":now(),\"version\":\"v115.0.0\",\"ok\":True,\"actions\":[\"active-window\",\"open-url\",\"open-app\",\"speak\",\"type --yes\",\"press --yes\"]}\nif __name__==\"__main__\":\n    import sys; a=sys.argv[1] if len(sys.argv)>1 else \"status\"; print(json.dumps(status() if a==\"status\" else run(a,\" \".join(x for x in sys.argv[2:] if x!='--yes'),\"--yes\" in sys.argv),indent=4,ensure_ascii=False))\n", "seed_screen_understanding_v116.py": "import json,hashlib\nfrom pathlib import Path\nfrom datetime import datetime\nSTATE=Path(\"seed_screen_understanding_v116_state.json\")\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef observe():\n    import seed_operator2_v115 as op; aw=op.active_window(); fp=hashlib.sha1(json.dumps(aw,sort_keys=True).encode()).hexdigest()[:12]; prev={}\n    if STATE.exists():\n        try: prev=json.loads(STATE.read_text(errors='ignore'))\n        except Exception: pass\n    r={\"created_at\":now(),\"version\":\"v116.0.0\",\"ok\":True,\"fingerprint\":fp,\"changed\":prev.get(\"fingerprint\")!=fp,\"active_window\":aw,\"privacy\":\"no raw screenshot saved\"}; STATE.write_text(json.dumps(r,indent=4)); return r\ndef status(): return {\"created_at\":now(),\"version\":\"v116.0.0\",\"ok\":True,\"current\":observe()}\nif __name__==\"__main__\":\n    import json; print(json.dumps(status(),indent=4,ensure_ascii=False))\n", "seed_approval_ui2_v117.py": "import json\nfrom pathlib import Path\nfrom datetime import datetime,timedelta\nPOLICY=Path(\"seed_approval_policy_v117.jsonl\")\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef write(r):\n    r.setdefault(\"created_at\",now()); r.setdefault(\"version\",\"v117.0.0\")\n    with POLICY.open(\"a\") as f:f.write(json.dumps(r,ensure_ascii=False)+\"\\n\")\n    return r\ndef read():\n    if not POLICY.exists(): return []\n    out=[]\n    for l in POLICY.read_text(errors='ignore').splitlines():\n        try: out.append(json.loads(l))\n        except Exception: pass\n    return out\ndef approve_once(a): return {\"ok\":True,\"policy\":write({\"kind\":\"approve_once\",\"action\":a})}\ndef approve_minutes(a,m=10): return {\"ok\":True,\"policy\":write({\"kind\":\"approve_until\",\"action\":a,\"until\":(datetime.now()+timedelta(minutes=int(m))).isoformat(timespec='seconds')})}\ndef status():\n    try:\n        import seed_action_approval_v107 as aa; center=aa.status()\n    except Exception as e:center={\"ok\":False,\"error\":str(e)}\n    return {\"created_at\":now(),\"version\":\"v117.0.0\",\"ok\":True,\"policy_count\":len(read()),\"approval_center\":center}\nif __name__==\"__main__\":\n    import sys; a=sys.argv[1] if len(sys.argv)>1 else \"status\"; print(json.dumps(approve_once(\" \".join(sys.argv[2:])) if a==\"once\" else approve_minutes(sys.argv[2],sys.argv[3] if len(sys.argv)>3 else 10) if a==\"minutes\" else status(),indent=4,ensure_ascii=False))\n", "seed_skill_registry2_v118.py": "import json, subprocess, sys\nfrom pathlib import Path\nfrom datetime import datetime\nREG=Path(\"seed_skill_registry2_v118.json\")\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef risk(n):\n    n=n.lower(); return \"risky\" if any(x in n for x in [\"delete\",\"shell\",\"operator\",\"mcp\",\"email\",\"write\"]) else \"safe\" if any(x in n for x in [\"open\",\"speak\",\"screenshot\"]) else \"observe\"\ndef discover():\n    skills=[]\n    if Path(\"seed_skills\").exists():\n        for p in Path(\"seed_skills\").rglob(\"*.json\"):\n            try:\n                d=json.loads(p.read_text(errors='ignore')); skills.append({\"id\":d.get(\"id\",p.stem),\"name\":d.get(\"name\",p.stem),\"path\":str(p),\"risk\":d.get(\"risk\",risk(d.get(\"name\",p.stem)))})\n            except Exception as e: skills.append({\"id\":p.stem,\"path\":str(p),\"risk\":\"unknown\",\"error\":str(e)})\n    for p in Path(\".\").glob(\"seed_*_v*.py\"): skills.append({\"id\":p.stem,\"name\":p.stem,\"path\":str(p),\"risk\":risk(p.stem)})\n    data={\"created_at\":now(),\"version\":\"v118.0.0\",\"ok\":True,\"count\":len(skills),\"skills\":skills}; REG.write_text(json.dumps(data,indent=4,ensure_ascii=False)); return data\ndef status():\n    if not REG.exists(): return discover()\n    try: return json.loads(REG.read_text(errors='ignore'))\n    except Exception: return discover()\nif __name__==\"__main__\":\n    import sys; print(json.dumps(discover() if len(sys.argv)>1 and sys.argv[1]==\"discover\" else status(),indent=4,ensure_ascii=False))\n", "seed_mcp_bridge_v119.py": "import json\nfrom pathlib import Path\nfrom datetime import datetime\nREG=Path(\"seed_mcp_registry_v119.json\"); CALLS=Path(\"seed_mcp_calls_v119.jsonl\")\nDEFAULT={\"version\":\"v119.0.0\",\"enabled\":False,\"allow_execute\":False,\"local_only\":True,\"servers\":{}}\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef registry():\n    if REG.exists():\n        try:\n            d=DEFAULT.copy(); d.update(json.loads(REG.read_text(errors='ignore'))); d[\"version\"]=\"v119.0.0\"; return d\n        except Exception: pass\n    REG.write_text(json.dumps(DEFAULT,indent=4)); return DEFAULT.copy()\ndef register(n,cmd): d=registry(); d[\"servers\"][n]={\"command\":cmd,\"enabled\":False,\"risk\":\"risky\"}; REG.write_text(json.dumps(d,indent=4)); return {\"ok\":True,\"server\":d[\"servers\"][n]}\ndef scan(): return {\"ok\":True,\"found\":[str(p) for p in [Path('mcp.json'),Path('.mcp.json'),Path('mcp_servers.json')] if p.exists()],\"registry\":registry()}\ndef status(): return {\"created_at\":now(),\"version\":\"v119.0.0\",\"ok\":True,\"registry\":registry(),\"scan\":scan()[\"found\"]}\nif __name__==\"__main__\":\n    import sys; print(json.dumps(register(sys.argv[2],\" \".join(sys.argv[3:])) if len(sys.argv)>1 and sys.argv[1]==\"register\" else scan() if len(sys.argv)>1 and sys.argv[1]==\"scan\" else status(),indent=4,ensure_ascii=False))\n", "seed_odysseus_audit_v120.py": "import json\nfrom pathlib import Path\nfrom datetime import datetime\nREPO=Path(\"third_party_repos/odysseus\"); MD=Path(\"seed_odysseus_lessons_v120.md\")\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef audit():\n    lessons=[]; files=[]\n    if REPO.exists():\n        for n in [\"README.md\",\"ROADMAP.md\",\"SECURITY.md\",\"THREAT_MODEL.md\"]:\n            p=REPO/n\n            if p.exists():\n                t=p.read_text(errors='ignore')[:6000].lower(); files.append(str(p))\n                if \"security\" in t or \"threat\" in t: lessons.append(\"Use Odysseus threat/security ideas for Seed tool approval.\")\n                if \"mcp\" in t: lessons.append(\"Keep MCP whitelist + approval only.\")\n                if \"research\" in t: lessons.append(\"Research flow can inspire Seed deep research.\")\n    else: lessons.append(\"Odysseus not cloned yet; audit-only clone later.\")\n    data={\"created_at\":now(),\"version\":\"v120.0.0\",\"ok\":True,\"repo_exists\":REPO.exists(),\"files\":files,\"lessons\":sorted(set(lessons))}; MD.write_text(\"# Seed v120 Odysseus Audit\\n\"+\"\\n\".join(\"- \"+x for x in data[\"lessons\"])+\"\\n\"); return data\ndef status(): return audit()\nif __name__==\"__main__\":\n    import json; print(json.dumps(audit(),indent=4,ensure_ascii=False))\n", "seed_repo_audit_v121.py": "import json\nfrom pathlib import Path\nfrom datetime import datetime\nROOT=Path(\"third_party_repos\"); MD=Path(\"seed_repo_audit_v121.md\")\nTARGETS=[\"screen-voice-agent\",\"openWakeWord\",\"whisper.cpp\",\"piper\",\"mem0\",\"letta\",\"leon\",\"nanobot\",\"mac-echo\",\"OpenBlob\",\"skales\",\"QwenPaw\",\"ora\",\"PikoChan\",\"odysseus\"]\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef audit_repo(p):\n    txt=\"\"; rs=list(p.glob(\"README*\"))\n    if rs:\n        try: txt=rs[0].read_text(errors='ignore')[:3000]\n        except Exception: pass\n    low=(txt+\" \"+p.name).lower(); cats=[c for k,c in [(\"wake\",\"wake\"),(\"voice\",\"voice\"),(\"memory\",\"memory\"),(\"mcp\",\"mcp\"),(\"desktop\",\"desktop\"),(\"agent\",\"agent\"),(\"research\",\"research\")] if k in low]\n    return {\"name\":p.name,\"path\":str(p),\"categories\":cats,\"has_readme\":bool(rs)}\ndef audit():\n    repos=[audit_repo(p) for p in ROOT.iterdir() if p.is_dir()] if ROOT.exists() else []\n    missing=[x for x in TARGETS if not any(x.lower() in r[\"name\"].lower() for r in repos)]\n    data={\"created_at\":now(),\"version\":\"v121.0.0\",\"ok\":True,\"repo_count\":len(repos),\"repos\":repos,\"missing_priority_repos\":missing}\n    MD.write_text(\"# Seed v121 Repo Audit\\n\\n\"+\"\\n\".join(\"- \"+r[\"name\"]+\": \"+(\",\".join(r[\"categories\"]) or \"uncategorized\") for r in repos)+\"\\n\"); return data\ndef status(): return audit()\nif __name__==\"__main__\":\n    import json; print(json.dumps(audit(),indent=4,ensure_ascii=False))\n", "seed_private_rag2_v122.py": "import json,re\nfrom pathlib import Path\nfrom datetime import datetime\nINDEX=Path(\"seed_private_rag2_v122_index.jsonl\")\nSETTINGS=Path(\"seed_private_rag2_v122_settings.json\")\nDEFAULT={\"version\":\"v122.0.0\",\"extensions\":[\".py\",\".md\",\".txt\",\".json\"],\"max_file_kb\":220,\"chunk_chars\":1200,\"modes\":{\"seed\":{\"roots\":[\".\"],\"exclude_dirs\":[\"third_party_repos\",\"seed_checkpoints\",\".git\",\"__pycache__\",\"node_modules\",\"venv\",\".venv\"]},\"audit\":{\"roots\":[\"third_party_repos\"],\"exclude_dirs\":[\".git\",\"node_modules\",\"venv\",\".venv\"]}}}\ndef now(): return datetime.now().isoformat(timespec=\"seconds\")\ndef settings():\n    if SETTINGS.exists():\n        try:\n            d=DEFAULT.copy(); d.update(json.loads(SETTINGS.read_text(errors='ignore'))); d[\"version\"]=\"v122.0.0\"; return d\n        except Exception: pass\n    SETTINGS.write_text(json.dumps(DEFAULT,indent=4)); return DEFAULT.copy()\ndef index(mode=\"seed\"):\n    s=settings(); m=s[\"modes\"].get(mode,s[\"modes\"][\"seed\"]); rows=[]\n    for root in m[\"roots\"]:\n        if not Path(root).exists(): continue\n        for p in Path(root).rglob(\"*\"):\n            try:\n                if set(p.parts)&set(m.get(\"exclude_dirs\",[])): continue\n                if not p.is_file() or p.suffix not in set(s[\"extensions\"]) or p.stat().st_size>int(s[\"max_file_kb\"])*1024: continue\n                txt=p.read_text(errors='ignore'); n=int(s[\"chunk_chars\"])\n                for i in range(0,len(txt),n): rows.append({\"mode\":mode,\"path\":str(p),\"chunk\":i//n,\"text\":txt[i:i+n],\"modified_at\":datetime.fromtimestamp(p.stat().st_mtime).isoformat(timespec='seconds'),\"indexed_at\":now()})\n            except Exception: pass\n    INDEX.write_text(\"\\n\".join(json.dumps(r,ensure_ascii=False) for r in rows)+(\"\\n\" if rows else \"\")); return {\"ok\":True,\"version\":\"v122.0.0\",\"mode\":mode,\"chunks\":len(rows)}\ndef rows():\n    if not INDEX.exists(): return []\n    out=[]\n    for l in INDEX.read_text(errors='ignore').splitlines():\n        try: out.append(json.loads(l))\n        except Exception: pass\n    return out\ndef search(q,limit=10):\n    terms=[x for x in re.sub(r\"[^a-z0-9çğıöşü\\s]\",\" \",q.lower()).split() if len(x)>1]; hits=[]\n    for r in rows():\n        hay=(r[\"path\"]+\" \"+r[\"text\"]).lower(); sc=sum(hay.count(t) for t in terms)\n        if sc>0: hits.append({\"score\":sc,\"source\":f\"{r['path']}#chunk={r['chunk']}\",\"preview\":r[\"text\"][:450],\"modified_at\":r[\"modified_at\"]})\n    return sorted(hits,key=lambda x:x[\"score\"],reverse=True)[:int(limit)]\ndef status():\n    rs=rows(); by={}\n    for r in rs: by[r.get(\"mode\",\"unknown\")]=by.get(r.get(\"mode\",\"unknown\"),0)+1\n    return {\"created_at\":now(),\"version\":\"v122.0.0\",\"ok\":True,\"chunks\":len(rs),\"by_mode\":by,\"settings\":settings()}\nif __name__==\"__main__\":\n    import sys\n    a=sys.argv[1] if len(sys.argv)>1 else \"status\"\n    print(json.dumps(index(sys.argv[2] if len(sys.argv)>2 else \"seed\") if a==\"index\" else search(\" \".join(sys.argv[2:])) if a==\"search\" else status(),indent=4,ensure_ascii=False))\n", "seed_v108_122_systems.py": "import json\nfrom datetime import datetime\nSYSTEMS=[(\"Proactive\",\"seed_proactive_rhythm_v108\",\"status\"),(\"Native Wake\",\"seed_native_wake_v109\",\"status\"),(\"STT\",\"seed_stt_v110\",\"status\"),(\"TTS\",\"seed_tts_v111\",\"status\"),(\"Memory3\",\"seed_memory_garden3_v112\",\"status\"),(\"Memory Gate\",\"seed_memory_gate_v113\",\"status\"),(\"Project Memory\",\"seed_project_memory_v114\",\"status\"),(\"Operator2\",\"seed_operator2_v115\",\"status\"),(\"Screen2\",\"seed_screen_understanding_v116\",\"status\"),(\"ApprovalUI2\",\"seed_approval_ui2_v117\",\"status\"),(\"Skills2\",\"seed_skill_registry2_v118\",\"status\"),(\"MCP\",\"seed_mcp_bridge_v119\",\"status\"),(\"Odysseus\",\"seed_odysseus_audit_v120\",\"status\"),(\"RepoAudit\",\"seed_repo_audit_v121\",\"status\"),(\"RAG2\",\"seed_private_rag2_v122\",\"status\")]\ndef now(): return datetime.now().isoformat(timespec='seconds')\ndef status():\n    cards=[]\n    for t,m,f in SYSTEMS:\n        try:\n            mod=__import__(m,fromlist=[f]); d=getattr(mod,f)(); cards.append({\"title\":t,\"ok\":bool(d.get(\"ok\",True)),\"data\":d})\n        except Exception as e: cards.append({\"title\":t,\"ok\":False,\"error\":str(e)})\n    return {\"created_at\":now(),\"version\":\"v108-v122.0.0\",\"ok\":all(c[\"ok\"] for c in cards),\"ok_count\":sum(c[\"ok\"] for c in cards),\"total\":len(cards),\"cards\":cards}\nif __name__==\"__main__\": print(json.dumps(status(),indent=4,ensure_ascii=False))\n", "seed_v108_122_gate.py": "import json,subprocess,sys\nfrom pathlib import Path\nfrom datetime import datetime\nMODULES=[\"seed_proactive_rhythm_v108.py\",\"seed_native_wake_v109.py\",\"seed_stt_v110.py\",\"seed_tts_v111.py\",\"seed_memory_garden3_v112.py\",\"seed_memory_gate_v113.py\",\"seed_project_memory_v114.py\",\"seed_operator2_v115.py\",\"seed_screen_understanding_v116.py\",\"seed_approval_ui2_v117.py\",\"seed_skill_registry2_v118.py\",\"seed_mcp_bridge_v119.py\",\"seed_odysseus_audit_v120.py\",\"seed_repo_audit_v121.py\",\"seed_private_rag2_v122.py\",\"seed_v108_122_systems.py\",\"seed_v108_122_mega.py\",\"seed_v108_122_gate.py\"]\ndef now(): return datetime.now().isoformat(timespec='seconds')\ndef comp(m):\n    p=subprocess.run([sys.executable,\"-m\",\"py_compile\",m],capture_output=True,text=True,timeout=30); return {\"module\":m,\"ok\":p.returncode==0,\"stderr\":p.stderr[-1000:]}\ndef run_gate():\n    checks=[comp(m) for m in MODULES]; modules_ok=all(c[\"ok\"] for c in checks); details={}\n    def get(name,mod,fn):\n        try:\n            d=getattr(__import__(mod,fromlist=[fn]),fn)(); details[name]=d; return bool(d.get(\"ok\",True))\n        except Exception as e: details[name]={\"ok\":False,\"error\":str(e)}; return False\n    tests=[get(\"proactive\",\"seed_proactive_rhythm_v108\",\"test\"),get(\"wake\",\"seed_native_wake_v109\",\"test\"),get(\"stt\",\"seed_stt_v110\",\"test\"),get(\"tts\",\"seed_tts_v111\",\"status\"),get(\"memory3\",\"seed_memory_garden3_v112\",\"status\"),get(\"memory_gate\",\"seed_memory_gate_v113\",\"test\"),get(\"project\",\"seed_project_memory_v114\",\"test\"),get(\"operator2\",\"seed_operator2_v115\",\"status\"),get(\"screen2\",\"seed_screen_understanding_v116\",\"status\"),get(\"approval2\",\"seed_approval_ui2_v117\",\"status\"),get(\"skills2\",\"seed_skill_registry2_v118\",\"discover\"),get(\"mcp\",\"seed_mcp_bridge_v119\",\"status\"),get(\"odysseus\",\"seed_odysseus_audit_v120\",\"status\"),get(\"repo\",\"seed_repo_audit_v121\",\"status\"),get(\"rag2\",\"seed_private_rag2_v122\",\"status\"),get(\"systems\",\"seed_v108_122_systems\",\"status\")]\n    r={\"created_at\":now(),\"version\":\"v108-v122.0.0\",\"ready\":modules_ok and all(tests),\"modules_ok\":modules_ok,\"tests_ok\":all(tests),\"details\":details,\"checks\":checks}\n    Path(\"seed_v108_122_gate_report.json\").write_text(json.dumps(r,indent=4,ensure_ascii=False)); return r\ndef show():\n    r=run_gate(); print(\"\\n=== SEED v108-v122 MEGA EVOLUTION GATE ===\"); print(f\"Ready: {r['ready']}\"); print(f\"Modules OK: {r['modules_ok']}\"); print(f\"Tests OK: {r['tests_ok']}\"); print(f\"Systems: {r['details'].get('systems',{}).get('ok_count')}/{r['details'].get('systems',{}).get('total')}\")\nif __name__==\"__main__\": show()\n", "seed_v108_122_mega.py": "import json,sys\ndef main():\n    a=sys.argv[1] if len(sys.argv)>1 else \"status\"; rest=sys.argv[2:]\n    if a==\"status\":\n        import seed_v108_122_systems as s; print(json.dumps(s.status(),indent=4,ensure_ascii=False))\n    elif a==\"gate\":\n        import seed_v108_122_gate as g; g.show()\n    elif a==\"proactive\":\n        import seed_proactive_rhythm_v108 as p; sub=rest[0] if rest else \"status\"; print(json.dumps(p.start(\"--speak\" in rest) if sub==\"start\" else p.stop() if sub==\"stop\" else p.tick(\"--speak\" in rest) if sub==\"tick\" else p.status(),indent=4,ensure_ascii=False))\n    elif a==\"rag2\":\n        import seed_private_rag2_v122 as r; sub=rest[0] if rest else \"status\"; print(json.dumps(r.index(rest[1] if len(rest)>1 else \"seed\") if sub==\"index\" else r.search(\" \".join(rest[1:])) if sub==\"search\" else r.status(),indent=4,ensure_ascii=False))\n    else: print(\"Commands: status | gate | proactive start/stop/tick/status | rag2 index/search/status\")\nif __name__==\"__main__\": main()\n", "seed_dashboard_v106.py": "import json,os,signal,subprocess,sys\nfrom datetime import datetime\nfrom http.server import BaseHTTPRequestHandler,HTTPServer\nfrom pathlib import Path\nfrom urllib.parse import urlparse\nPID=Path(\"seed_dashboard_v106.pid\"); LOG=Path(\"seed_dashboard_v106.log\"); PORT=8806\ndef now(): return datetime.now().isoformat(timespec='seconds')\nCARDS=[(\"Supervisor\",\"seed_supervisor_v92\",\"supervisor_status\"),(\"Safety\",\"seed_safety_ledger_v94\",\"status\"),(\"Approval\",\"seed_action_approval_v107\",\"status\"),(\"Proactive\",\"seed_proactive_rhythm_v108\",\"status\"),(\"Native Wake\",\"seed_native_wake_v109\",\"status\"),(\"STT\",\"seed_stt_v110\",\"status\"),(\"TTS\",\"seed_tts_v111\",\"status\"),(\"Memory3\",\"seed_memory_garden3_v112\",\"status\"),(\"Memory Gate\",\"seed_memory_gate_v113\",\"status\"),(\"Project Memory\",\"seed_project_memory_v114\",\"status\"),(\"Operator2\",\"seed_operator2_v115\",\"status\"),(\"Screen2\",\"seed_screen_understanding_v116\",\"status\"),(\"ApprovalUI2\",\"seed_approval_ui2_v117\",\"status\"),(\"Skills2\",\"seed_skill_registry2_v118\",\"status\"),(\"MCP\",\"seed_mcp_bridge_v119\",\"status\"),(\"Odysseus\",\"seed_odysseus_audit_v120\",\"status\"),(\"RepoAudit\",\"seed_repo_audit_v121\",\"status\"),(\"RAG2\",\"seed_private_rag2_v122\",\"status\"),(\"Doctor\",\"seed_doctor_v105\",\"diagnose\")]\ndef one(t,m,f):\n    try:\n        mod=__import__(m,fromlist=[f]); d=getattr(mod,f)(); return {\"title\":t,\"ok\":bool(d.get(\"ok\",True)),\"summary\":summ(t,d),\"data\":d}\n    except Exception as e: return {\"title\":t,\"ok\":False,\"summary\":str(e),\"data\":{\"error\":str(e)}}\ndef summ(t,d):\n    if t==\"Proactive\": return f\"alive={d.get('alive')} asks={d.get('today_asks')}\"\n    if t==\"Native Wake\": return f\"route={d.get('route')}\"\n    if t==\"STT\": return f\"ready={d.get('ready')}\"\n    if t==\"Memory3\": return f\"memories={d.get('count')}\"\n    if t==\"RAG2\": return f\"chunks={d.get('chunks')}\"\n    if t==\"Approval\": return f\"pending={d.get('pending_count',0)}\"\n    return \"OK\" if d.get(\"ok\",True) else \"CHECK\"\ndef view_model():\n    cards=[one(*x) for x in CARDS]; return {\"created_at\":now(),\"version\":\"v122-dashboard\",\"ok_count\":sum(c[\"ok\"] for c in cards),\"total\":len(cards),\"ok\":sum(c[\"ok\"] for c in cards)>=16,\"cards\":cards}\nHTML='''<!doctype html><html><head><meta charset=\"utf-8\"><title>Seed v122 Dashboard</title><style>body{font-family:-apple-system;background:#080808;color:#eee;margin:0}header{padding:24px 34px;background:#111;border-bottom:1px solid #333;position:sticky;top:0}main{padding:24px 34px}.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:14px}.card{background:#151515;border:1px solid #333;border-radius:18px;padding:16px}.ok{color:#65e88a}.bad{color:#ff5d5d}pre{white-space:pre-wrap;background:#050505;border:1px solid #222;border-radius:12px;padding:10px;max-height:240px;overflow:auto}.muted{color:#aaa}</style></head><body><header><h1>Seed v108-v122 Dashboard</h1><div id=s class=muted>loading</div></header><main><h2 id=score></h2><div id=g class=grid></div></main><script>function esc(x){return String(x??'').replace(/[&<>]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[m]))}async function r(){let d=await(await fetch('/api/status?'+Date.now())).json();s.textContent=d.created_at;score.textContent=d.ok_count+'/'+d.total+' green';g.innerHTML=d.cards.map(c=>`<div class=card><h3 class=${c.ok?'ok':'bad'}>${esc(c.title)} ${c.ok?'OK':'CHECK'}</h3><p class=muted>${esc(c.summary)}</p><details><summary>details</summary><pre>${esc(JSON.stringify(c.data,null,2))}</pre></details></div>`).join('')}r();setInterval(r,4000)</script></body></html>'''\nclass H(BaseHTTPRequestHandler):\n    def do_GET(self):\n        p=urlparse(self.path).path\n        if p in [\"/api/status\",\"/status\"]:\n            b=json.dumps(view_model(),ensure_ascii=False).encode(); self.send_response(200); self.send_header(\"Content-Type\",\"application/json; charset=utf-8\"); self.end_headers(); self.wfile.write(b)\n        else:\n            b=HTML.encode(); self.send_response(200); self.send_header(\"Content-Type\",\"text/html; charset=utf-8\"); self.end_headers(); self.wfile.write(b)\n    def log_message(self,*a): return\ndef alive(pid):\n    try: os.kill(int(pid),0); return True\n    except Exception: return False\ndef serve(): HTTPServer((\"127.0.0.1\",PORT),H).serve_forever()\ndef start():\n    if PID.exists():\n        try:\n            pid=int(PID.read_text())\n            if alive(pid): subprocess.Popen([\"open\",f\"http://127.0.0.1:{PORT}/?v=122\"]); return {\"ok\":True,\"already_running\":True,\"pid\":pid,\"url\":f\"http://127.0.0.1:{PORT}/\"}\n        except Exception: pass\n    p=subprocess.Popen([sys.executable,\"seed_dashboard_v106.py\",\"serve\"],stdout=LOG.open(\"a\"),stderr=LOG.open(\"a\")); PID.write_text(str(p.pid)); subprocess.Popen([\"open\",f\"http://127.0.0.1:{PORT}/?v=122\"]); return {\"ok\":True,\"pid\":p.pid,\"url\":f\"http://127.0.0.1:{PORT}/\"}\ndef stop():\n    pid=None; st=False\n    if PID.exists():\n        try:\n            pid=int(PID.read_text())\n            if alive(pid): os.kill(pid,signal.SIGTERM); st=True\n            PID.unlink(missing_ok=True)\n        except Exception: pass\n    return {\"ok\":True,\"stopped\":st,\"pid\":pid}\ndef status(): return {\"created_at\":now(),\"version\":\"v122-dashboard\",\"ok\":True,\"alive\":PID.exists(),\"url\":f\"http://127.0.0.1:{PORT}/\"}\nif __name__==\"__main__\":\n    a=sys.argv[1] if len(sys.argv)>1 else \"status\"; print(json.dumps(start() if a==\"start\" else stop() if a==\"stop\" else status(),indent=4,ensure_ascii=False)) if a!=\"serve\" else serve()\n", "seed_natural_intent_router_v108_122.py": "def norm(s): return ' '.join(str(s or '').lower().split())\ndef handle_natural_intent_v108_122(user_message):\n    t=norm(user_message)\n    if t in {'v122 status','mega status','evolution status'}:\n        import seed_v108_122_systems as s; print(s.status()); return 'handled'\n    if t in {'proactive tick','seed check in'}:\n        import seed_proactive_rhythm_v108 as p; print(p.tick()); return 'handled'\n    return None\n"}

for path, text in FILES.items():
    Path(path).write_text(text)
    print("Wrote", path)

# Patch seed_commands
cmdp=Path("seed_commands.py")
cmd=cmdp.read_text(errors="ignore") if cmdp.exists() else "def handle_chat_command(user_message,*args,**kwargs): return None\n"
if "_seed_v108_122_previous_handle_chat_command" not in cmd:
    cmd += """
# v108-v122 mega evolution natural router.
try:
    _seed_v108_122_previous_handle_chat_command = handle_chat_command
    def handle_chat_command(user_message, *args, **kwargs):
        try:
            from seed_natural_intent_router_v108_122 import handle_natural_intent_v108_122
            handled = handle_natural_intent_v108_122(user_message)
            if handled == "handled":
                return "handled"
        except Exception as error:
            print(f"v108-v122 natural router error: {error}")
            return "handled"
        return _seed_v108_122_previous_handle_chat_command(user_message, *args, **kwargs)
except Exception:
    pass
"""
    cmdp.write_text(cmd); print("Patched seed_commands.py")

# Config
cfgp=Path("seed_config.py")
cfg=cfgp.read_text(errors="ignore") if cfgp.exists() else 'SEED_VERSION = "v108-v122.0.0"\n'
cfg=re.sub(r'^SEED_VERSION\s*=\s*".*?"','SEED_VERSION = "v108-v122.0.0"',cfg,flags=re.M)
for name in ["SEED_V108_PROACTIVE_RHYTHM","SEED_V109_NATIVE_WAKE","SEED_V110_STT","SEED_V111_TTS","SEED_V112_MEMORY_GARDEN3","SEED_V113_MEMORY_GATE","SEED_V114_PROJECT_MEMORY","SEED_V115_OPERATOR2","SEED_V116_SCREEN_UNDERSTANDING2","SEED_V117_APPROVAL_UI2","SEED_V118_SKILL_REGISTRY2","SEED_V119_SAFE_MCP","SEED_V120_ODYSSEUS_AUDIT","SEED_V121_REPO_AUDIT","SEED_V122_PRIVATE_RAG2"]:
    if name not in cfg: cfg += f"\n{name} = True"
cfgp.write_text(cfg+"\n"); print("Updated seed_config.py")

# Core notes
core=Path("Seed_Core.md")
ct=core.read_text(errors="ignore") if core.exists() else ""
if "Seed v108-v122 Mega Evolution" not in ct:
    ct += """
## Seed v108-v122 Mega Evolution

Adds proactive companion rhythm, native wake manager, STT/TTS wrappers, Memory Garden 3, Memory Gate, Project Memory, Operator 2, Screen Understanding 2, Approval UI 2, Skill Registry 2, Safe MCP Bridge, Odysseus/Repo audits, and Private RAG 2.
"""
core.write_text(ct); print("Updated Seed_Core.md")

# Gitignore
gi=Path(".gitignore")
g=gi.read_text(errors="ignore") if gi.exists() else ""
block="\n# Seed v108-v122 runtime\nseed_proactive_v108_events.jsonl\nseed_proactive_v108.pid\nseed_proactive_v108.log\nseed_memory_garden3_v112.jsonl\nseed_project_memory_v114.jsonl\nseed_approval_policy_v117.jsonl\nseed_skill_registry2_v118.json\nseed_mcp_registry_v119.json\nseed_odysseus_lessons_v120.md\nseed_repo_audit_v121.md\nseed_private_rag2_v122_index.jsonl\nseed_v108_122_gate_report.json\n"
if "Seed v108-v122 runtime" not in g:
    gi.write_text(g+block); print("Updated .gitignore")

targets=list(FILES.keys())+["seed_commands.py"]
for m in targets:
    p=subprocess.run([sys.executable,"-m","py_compile",m],capture_output=True,text=True,timeout=30)
    print("$ python -m py_compile",m)
    if p.returncode==0: print("OK")
    else:
        print(p.stderr); sys.exit(p.returncode)

init_cmds=[
    [sys.executable,"seed_memory_garden3_v112.py","import"],
    [sys.executable,"seed_skill_registry2_v118.py","discover"],
    [sys.executable,"seed_odysseus_audit_v120.py"],
    [sys.executable,"seed_repo_audit_v121.py"],
    [sys.executable,"seed_private_rag2_v122.py","index","seed"],
]
for c in init_cmds:
    try:
        p=subprocess.run(c,capture_output=True,text=True,timeout=180)
        print("$"," ".join(c)); print(p.stdout.strip()[:4000])
        if p.stderr.strip(): print(p.stderr.strip()[:1000])
    except Exception as e: print("init warning",c,e)

print("\nSeed v108-v122 Mega Evolution installed.")
