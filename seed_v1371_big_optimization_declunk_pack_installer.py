#!/usr/bin/env python3
from pathlib import Path
import subprocess, sys, re, json

FILES={"seed_output_compactor_v1371.py": "import json, re, sys\nfrom pathlib import Path\nfrom datetime import datetime\n\nVERSION = \"v137.1.0\"\nEVENTS = Path(\"seed_output_compactor_v1371_events.jsonl\")\nFULL_OUTPUT_DIR = Path(\"seed_full_outputs_v1371\")\nFULL_OUTPUT_DIR.mkdir(exist_ok=True)\n\ndef now():\n    return datetime.now().isoformat(timespec=\"seconds\")\n\ndef event(row):\n    row.setdefault(\"created_at\", now())\n    row.setdefault(\"version\", VERSION)\n    with EVENTS.open(\"a\") as f:\n        f.write(json.dumps(row, ensure_ascii=False) + \"\\n\")\n    return row\n\ndef _loads_maybe(text):\n    if not isinstance(text, str):\n        return text\n    t = text.strip()\n    if not t:\n        return None\n    # Try whole JSON first.\n    try:\n        return json.loads(t)\n    except Exception:\n        pass\n    # Try finding first object.\n    start = t.find(\"{\")\n    end = t.rfind(\"}\")\n    if start >= 0 and end > start:\n        try:\n            return json.loads(t[start:end+1])\n        except Exception:\n            return None\n    return None\n\ndef _walk(obj):\n    if isinstance(obj, dict):\n        yield obj\n        for v in obj.values():\n            yield from _walk(v)\n    elif isinstance(obj, list):\n        for v in obj:\n            yield from _walk(v)\n\ndef _first_str(obj, keys):\n    for d in _walk(obj):\n        for k in keys:\n            v = d.get(k) if isinstance(d, dict) else None\n            if isinstance(v, str) and v.strip():\n                return v.strip()\n    return None\n\ndef _first_obj(obj, keys):\n    for d in _walk(obj):\n        for k in keys:\n            v = d.get(k) if isinstance(d, dict) else None\n            if isinstance(v, (dict, list)):\n                return v\n    return None\n\ndef _regex_first(text, patterns):\n    for pat in patterns:\n        m = re.search(pat, text, flags=re.S)\n        if m:\n            val = m.group(1)\n            try:\n                return json.loads('\"' + val.replace('\"', '\\\\\"') + '\"')\n            except Exception:\n                return val.replace(\"\\\\n\", \"\\n\").replace('\\\\\"', '\"').strip()\n    return None\n\ndef save_full_output(stdout=\"\", stderr=\"\", meta=None):\n    meta = meta or {}\n    name = datetime.now().strftime(\"full_output_%Y%m%d_%H%M%S_%f.json\")\n    path = FULL_OUTPUT_DIR / name\n    path.write_text(json.dumps({\n        \"created_at\": now(),\n        \"version\": VERSION,\n        \"meta\": meta,\n        \"stdout\": stdout,\n        \"stderr\": stderr,\n    }, indent=4, ensure_ascii=False))\n    return str(path)\n\ndef compact(stdout=\"\", stderr=\"\", returncode=0, meta=None, max_chars=1800):\n    meta = meta or {}\n    full_path = save_full_output(stdout, stderr, meta)\n    data = _loads_maybe(stdout)\n    answer = None\n    intent = None\n    transcript = None\n    ok = returncode == 0\n    wake = None\n    if data is not None:\n        answer = _first_str(data, [\"answer\", \"final_answer\", \"response\", \"message\"])\n        intent = _first_str(data, [\"intent\", \"intent_name\"])\n        transcript = _first_str(data, [\"transcript\", \"raw_text\", \"input\"])\n        wake = _first_obj(data, [\"wake\"])\n        # Avoid treating long RAG previews as answer.\n        if answer and (\"seed_control_plane\" in answer or \"?? seed_\" in answer):\n            answer = None\n        for d in _walk(data):\n            if isinstance(d, dict) and d.get(\"ok\") is False:\n                ok = False\n                break\n    if answer is None and stdout:\n        answer = _regex_first(stdout, [\n            r'\"answer\"\\s*:\\s*\"([^\"]{1,2000})\"',\n            r\"'answer'\\s*:\\s*'([^']{1,2000})'\",\n            r\"Seed status:\\s*([^\\n\\r]{1,500})\",\n        ])\n        if answer and not str(answer).startswith(\"Seed status:\") and \"8/8\" in str(answer):\n            answer = \"Seed status: \" + str(answer).strip()\n    if intent is None and stdout:\n        intent = _regex_first(stdout, [r'\"intent\"\\s*:\\s*\"([^\"]{1,200})\"', r\"'intent'\\s*:\\s*'([^']{1,200})'\"])\n    if transcript is None and stdout:\n        transcript = _regex_first(stdout, [r'\"raw_text\"\\s*:\\s*\"([^\"]{1,500})\"', r'\"input\"\\s*:\\s*\"([^\"]{1,500})\"'])\n    if answer is None:\n        # Last resort: meaningful tail without huge JSON.\n        cleaned = re.sub(r\"\\s+\", \" \", stdout or \"\").strip()\n        answer = cleaned[:max_chars] if cleaned else \"\"\n    if len(answer) > max_chars:\n        answer = answer[:max_chars] + \"…\"\n    out = {\n        \"ok\": ok,\n        \"answer\": answer,\n        \"intent\": intent,\n        \"transcript\": transcript,\n        \"returncode\": returncode,\n        \"full_output_file\": full_path,\n    }\n    if stderr and returncode != 0:\n        out[\"stderr_tail\"] = stderr[-800:]\n    if wake is not None:\n        out[\"wake\"] = wake\n    event({\"event\": \"compact\", \"ok\": ok, \"intent\": intent, \"answer_preview\": answer[:160], \"full_output_file\": full_path})\n    return out\n\ndef text_summary(obj):\n    lines = []\n    lines.append(\"Seed Compact Runtime Output\")\n    lines.append(f\"OK: {obj.get('ok')}\")\n    if obj.get(\"intent\"):\n        lines.append(f\"Intent: {obj.get('intent')}\")\n    if obj.get(\"transcript\"):\n        lines.append(f\"Input: {obj.get('transcript')}\")\n    lines.append(f\"Answer: {obj.get('answer')}\")\n    lines.append(f\"Full output: {obj.get('full_output_file')}\")\n    return \"\\n\".join(lines)\n\nif __name__ == \"__main__\":\n    raw = sys.stdin.read()\n    c = compact(raw, \"\", 0, {\"source\": \"stdin\"})\n    if \"--json\" in sys.argv:\n        print(json.dumps(c, indent=4, ensure_ascii=False))\n    else:\n        print(text_summary(c))\n", "seed_runtime_proxy_v1371.py": "import json, subprocess, sys, shlex, time\nfrom pathlib import Path\nfrom datetime import datetime\nimport seed_output_compactor_v1371 as compactor\n\nVERSION = \"v137.1.0\"\nEVENTS = Path(\"seed_runtime_proxy_v1371_events.jsonl\")\n\ndef now():\n    return datetime.now().isoformat(timespec=\"seconds\")\n\ndef event(row):\n    row.setdefault(\"created_at\", now())\n    row.setdefault(\"version\", VERSION)\n    with EVENTS.open(\"a\") as f:\n        f.write(json.dumps(row, ensure_ascii=False) + \"\\n\")\n    return row\n\ndef run(cmd, timeout=180):\n    start = time.time()\n    try:\n        p = subprocess.run(cmd, capture_output=True, text=True, timeout=timeout)\n        latency = round(time.time() - start, 3)\n        c = compactor.compact(p.stdout, p.stderr, p.returncode, {\"cmd\": cmd, \"latency_seconds\": latency})\n        c[\"latency_seconds\"] = latency\n        c[\"cmd\"] = cmd\n        event({\"event\": \"run\", \"cmd\": cmd, \"ok\": c.get(\"ok\"), \"latency_seconds\": latency, \"answer_preview\": (c.get(\"answer\") or \"\")[:160]})\n        return c\n    except subprocess.TimeoutExpired as e:\n        out = {\"ok\": False, \"error\": \"timeout\", \"cmd\": cmd, \"timeout\": timeout}\n        event({\"event\": \"timeout\", \"cmd\": cmd, \"timeout\": timeout})\n        return out\n    except Exception as e:\n        out = {\"ok\": False, \"error\": str(e), \"cmd\": cmd}\n        event({\"event\": \"error\", \"cmd\": cmd, \"error\": str(e)})\n        return out\n\ndef py(script, *args):\n    return [sys.executable, script, *args]\n\ndef wake_text(text, speak=False):\n    if not Path(\"seed_voice_runtime_v136.py\").exists():\n        return {\"ok\": False, \"error\": \"seed_voice_runtime_v136.py missing\"}\n    phrase = text if text.lower().startswith((\"wake up\", \"hey seed\", \"seed \")) else \"wake up \" + text\n    args = [\"wake-text\", phrase]\n    if not speak:\n        args.append(\"--no-speak\")\n    return run(py(\"seed_voice_runtime_v136.py\", *args), timeout=240)\n\ndef voice_once(speak=False):\n    if not Path(\"seed_voice_runtime_v136.py\").exists():\n        return {\"ok\": False, \"error\": \"seed_voice_runtime_v136.py missing\"}\n    args = [\"voice-once\"]\n    if not speak:\n        args.append(\"--no-speak\")\n    return run(py(\"seed_voice_runtime_v136.py\", *args), timeout=240)\n\ndef status():\n    if Path(\"seed_voice_runtime_v136.py\").exists():\n        return run(py(\"seed_voice_runtime_v136.py\", \"status\"), timeout=60)\n    return {\"ok\": False, \"error\": \"seed_voice_runtime_v136.py missing\"}\n\ndef text(obj):\n    lines = [\"Seed Runtime Proxy v137.1\"]\n    lines.append(f\"OK: {obj.get('ok')}\")\n    if obj.get(\"latency_seconds\") is not None:\n        lines.append(f\"Latency: {obj.get('latency_seconds')}s\")\n    if obj.get(\"intent\"):\n        lines.append(f\"Intent: {obj.get('intent')}\")\n    if obj.get(\"answer\"):\n        lines.append(f\"Answer: {obj.get('answer')}\")\n    if obj.get(\"full_output_file\"):\n        lines.append(f\"Full output: {obj.get('full_output_file')}\")\n    if obj.get(\"error\"):\n        lines.append(f\"Error: {obj.get('error')}\")\n    return \"\\n\".join(lines)\n\nif __name__ == \"__main__\":\n    cmd = sys.argv[1] if len(sys.argv) > 1 else \"status\"\n    speak = \"--speak\" in sys.argv\n    if cmd in {\"wake-text\", \"ask\", \"text\"}:\n        msg = \" \".join([a for a in sys.argv[2:] if a != \"--speak\"]) or \"status\"\n        res = wake_text(msg, speak=speak)\n    elif cmd in {\"voice-once\", \"listen\"}:\n        res = voice_once(speak=speak)\n    else:\n        res = status()\n    if \"--json\" in sys.argv:\n        print(json.dumps(res, indent=4, ensure_ascii=False))\n    else:\n        print(text(res))\n", "seed_log_optimizer_v1371.py": "import json, os, shutil, sys, glob\nfrom pathlib import Path\nfrom datetime import datetime\n\nVERSION = \"v137.1.0\"\nREPORT = Path(\"seed_log_optimizer_v1371_report.json\")\nEVENTS = Path(\"seed_log_optimizer_v1371_events.jsonl\")\nARCHIVE_DIR = Path(\"seed_log_archive_v1371\")\n\nPATTERNS = [\n    \"*events.jsonl\", \"*.log\", \"seed_full_outputs_v1371/*.json\",\n    \"seed_companion_v137_inbox.jsonl\", \"seed_companion_v137_events.jsonl\"\n]\n\ndef now():\n    return datetime.now().isoformat(timespec=\"seconds\")\n\ndef event(row):\n    row.setdefault(\"created_at\", now())\n    row.setdefault(\"version\", VERSION)\n    with EVENTS.open(\"a\") as f:\n        f.write(json.dumps(row, ensure_ascii=False) + \"\\n\")\n    return row\n\ndef candidates():\n    out = []\n    for pat in PATTERNS:\n        for p in glob.glob(pat):\n            path = Path(p)\n            if not path.is_file():\n                continue\n            if path.name in {EVENTS.name, REPORT.name}:\n                continue\n            try:\n                out.append({\"path\": str(path), \"bytes\": path.stat().st_size, \"lines\": len(path.read_text(errors=\"ignore\").splitlines()) if path.stat().st_size < 8_000_000 else None})\n            except Exception:\n                out.append({\"path\": str(path), \"bytes\": path.stat().st_size, \"lines\": None})\n    return sorted(out, key=lambda x: x[\"bytes\"], reverse=True)\n\ndef trim_file(path, keep_lines=700, archive=True):\n    path = Path(path)\n    if not path.exists() or not path.is_file():\n        return {\"ok\": False, \"path\": str(path), \"error\": \"missing\"}\n    txt = path.read_text(errors=\"ignore\")\n    lines = txt.splitlines()\n    before = {\"bytes\": path.stat().st_size, \"lines\": len(lines)}\n    if len(lines) <= keep_lines:\n        return {\"ok\": True, \"path\": str(path), \"unchanged\": True, \"before\": before, \"after\": before}\n    archive_path = None\n    if archive:\n        ARCHIVE_DIR.mkdir(exist_ok=True)\n        archive_path = ARCHIVE_DIR / (path.name + \".\" + datetime.now().strftime(\"%Y%m%d_%H%M%S\") + \".bak\")\n        shutil.copy2(path, archive_path)\n    path.write_text(\"\\n\".join(lines[-keep_lines:]) + \"\\n\")\n    after = {\"bytes\": path.stat().st_size, \"lines\": len(path.read_text(errors=\"ignore\").splitlines())}\n    return {\"ok\": True, \"path\": str(path), \"before\": before, \"after\": after, \"archive\": str(archive_path) if archive_path else None}\n\ndef optimize(keep_lines=700, min_bytes=250_000, apply=False):\n    rows = candidates()\n    actions = []\n    for c in rows:\n        if c[\"bytes\"] >= min_bytes or (c.get(\"lines\") or 0) > keep_lines * 2:\n            if apply:\n                actions.append(trim_file(c[\"path\"], keep_lines=keep_lines, archive=True))\n            else:\n                actions.append({\"ok\": True, \"dry_run\": True, \"path\": c[\"path\"], \"bytes\": c[\"bytes\"], \"lines\": c.get(\"lines\")})\n    report = {\"created_at\": now(), \"version\": VERSION, \"ok\": True, \"apply\": apply, \"candidate_count\": len(rows), \"action_count\": len(actions), \"actions\": actions}\n    REPORT.write_text(json.dumps(report, indent=4, ensure_ascii=False))\n    event({\"event\": \"optimize\", \"apply\": apply, \"action_count\": len(actions)})\n    return report\n\ndef status():\n    rows = candidates()\n    total = sum(x[\"bytes\"] for x in rows)\n    return {\"created_at\": now(), \"version\": VERSION, \"ok\": True, \"file_count\": len(rows), \"total_bytes\": total, \"largest\": rows[:12]}\n\nif __name__ == \"__main__\":\n    cmd = sys.argv[1] if len(sys.argv) > 1 else \"status\"\n    if cmd == \"apply\":\n        print(json.dumps(optimize(apply=True), indent=4, ensure_ascii=False))\n    elif cmd in {\"dry-run\", \"plan\"}:\n        print(json.dumps(optimize(apply=False), indent=4, ensure_ascii=False))\n    else:\n        print(json.dumps(status(), indent=4, ensure_ascii=False))\n", "seed_service_supervisor_v1371.py": "import json, os, signal, subprocess, sys, time\nfrom pathlib import Path\nfrom datetime import datetime\n\nVERSION = \"v137.1.0\"\nEVENTS = Path(\"seed_service_supervisor_v1371_events.jsonl\")\nSTATE = Path(\"seed_service_supervisor_v1371_state.json\")\n\nSERVICES = {\n    \"voice_runtime\": {\"script\": \"seed_voice_runtime_v136.py\", \"start\": [\"start\", \"--no-speak\"], \"stop\": [\"stop\"], \"status\": [\"status\"], \"required\": True},\n    \"approval_autopilot\": {\"script\": \"seed_approval_autopilot_v13623.py\", \"start\": [\"start\"], \"stop\": [\"stop\"], \"status\": [\"status\"], \"required\": True},\n    \"companion\": {\"script\": \"seed_companion_v137.py\", \"start\": [\"start\"], \"stop\": [\"stop\"], \"status\": [\"status\"], \"required\": True},\n    \"dashboard\": {\"script\": \"seed_dashboard_v106.py\", \"start\": [\"start\"], \"stop\": [\"stop\"], \"status\": [\"status\"], \"required\": False},\n    \"avatar\": {\"script\": \"seed_avatar2_v129.py\", \"start\": [\"start\"], \"stop\": [\"stop\"], \"status\": [\"status\"], \"required\": False},\n    \"proactive\": {\"script\": \"seed_proactive_rhythm_v108.py\", \"start\": [\"start\"], \"stop\": [\"stop\"], \"status\": [\"status\"], \"required\": False},\n}\n\ndef now():\n    return datetime.now().isoformat(timespec=\"seconds\")\n\ndef event(row):\n    row.setdefault(\"created_at\", now())\n    row.setdefault(\"version\", VERSION)\n    with EVENTS.open(\"a\") as f:\n        f.write(json.dumps(row, ensure_ascii=False) + \"\\n\")\n    return row\n\ndef run(cmd, timeout=60):\n    try:\n        p=subprocess.run(cmd,capture_output=True,text=True,timeout=timeout)\n        data=None\n        if p.stdout.strip():\n            try: data=json.loads(p.stdout)\n            except Exception: data={\"raw\":p.stdout.strip()[-2000:]}\n        return {\"ok\":p.returncode==0,\"returncode\":p.returncode,\"stdout\":p.stdout[-2000:],\"stderr\":p.stderr[-1000:],\"data\":data}\n    except Exception as e:\n        return {\"ok\":False,\"error\":str(e)}\n\ndef py(script,*args):\n    return [sys.executable,script,*args]\n\ndef service_call(name, action):\n    spec=SERVICES[name]\n    if not Path(spec[\"script\"]).exists():\n        return {\"ok\": False, \"missing\": spec[\"script\"]}\n    return run(py(spec[\"script\"], *spec[action]))\n\ndef service_alive_from_status(res):\n    data=res.get(\"data\") if isinstance(res,dict) else None\n    if isinstance(data,dict):\n        if data.get(\"alive\") is True:\n            return True\n        if data.get(\"ok\") is True and data.get(\"pid\"):\n            return True\n        if data.get(\"already_running\") is True:\n            return True\n    return False\n\ndef status():\n    rows={}\n    for name in SERVICES:\n        res=service_call(name,\"status\")\n        rows[name]={\"status\":res,\"alive\":service_alive_from_status(res),\"required\":SERVICES[name][\"required\"]}\n    required_ok=all(v[\"alive\"] for k,v in rows.items() if v[\"required\"])\n    state={\"created_at\":now(),\"version\":VERSION,\"ok\":True,\"required_ok\":required_ok,\"services\":rows}\n    STATE.write_text(json.dumps(state,indent=4,ensure_ascii=False))\n    return state\n\ndef start(all_services=False):\n    actions=[]\n    order=[\"voice_runtime\",\"approval_autopilot\",\"dashboard\",\"avatar\",\"proactive\",\"companion\"] if all_services else [\"voice_runtime\",\"approval_autopilot\",\"companion\"]\n    for name in order:\n        if name in SERVICES:\n            actions.append({\"service\":name,\"result\":service_call(name,\"start\")})\n    event({\"event\":\"start\",\"all_services\":all_services,\"actions\":actions})\n    return {\"ok\":True,\"actions\":actions,\"status\":status()}\n\ndef stop(all_services=False):\n    actions=[]\n    order=[\"companion\",\"approval_autopilot\",\"voice_runtime\",\"proactive\",\"avatar\",\"dashboard\"] if all_services else [\"companion\",\"approval_autopilot\",\"voice_runtime\"]\n    for name in order:\n        if name in SERVICES:\n            actions.append({\"service\":name,\"result\":service_call(name,\"stop\")})\n    event({\"event\":\"stop\",\"all_services\":all_services,\"actions\":actions})\n    return {\"ok\":True,\"actions\":actions,\"status\":status()}\n\ndef heal():\n    st=status()\n    actions=[]\n    for name,row in st[\"services\"].items():\n        if SERVICES[name][\"required\"] and not row[\"alive\"]:\n            actions.append({\"service\":name,\"action\":\"restart\",\"result\":service_call(name,\"start\")})\n    after=status()\n    event({\"event\":\"heal\",\"actions\":actions,\"required_ok_after\":after.get(\"required_ok\")})\n    return {\"ok\":True,\"actions\":actions,\"after\":after}\n\ndef test():\n    return {\"ok\":True,\"start\":start(False),\"heal\":heal(),\"status\":status()}\n\nif __name__==\"__main__\":\n    cmd=sys.argv[1] if len(sys.argv)>1 else \"status\"\n    if cmd==\"start\":\n        print(json.dumps(start(\"--all\" in sys.argv),indent=4,ensure_ascii=False))\n    elif cmd==\"stop\":\n        print(json.dumps(stop(\"--all\" in sys.argv),indent=4,ensure_ascii=False))\n    elif cmd==\"heal\":\n        print(json.dumps(heal(),indent=4,ensure_ascii=False))\n    elif cmd==\"test\":\n        print(json.dumps(test(),indent=4,ensure_ascii=False))\n    else:\n        print(json.dumps(status(),indent=4,ensure_ascii=False))\n", "seed_clunkiness_audit_v1371.py": "import json, subprocess, sys, time, glob\nfrom pathlib import Path\nfrom datetime import datetime\n\nVERSION=\"v137.1.0\"\nREPORT=Path(\"seed_clunkiness_audit_v1371_report.json\")\n\ndef now():\n    return datetime.now().isoformat(timespec=\"seconds\")\n\ndef run(cmd, timeout=120):\n    start=time.time()\n    try:\n        p=subprocess.run(cmd,capture_output=True,text=True,timeout=timeout)\n        return {\"cmd\":cmd,\"ok\":p.returncode==0,\"returncode\":p.returncode,\"latency_seconds\":round(time.time()-start,3),\"stdout_bytes\":len(p.stdout or \"\"),\"stderr_bytes\":len(p.stderr or \"\"),\"stdout_tail\":(p.stdout or \"\")[-1200:],\"stderr_tail\":(p.stderr or \"\")[-800:]}\n    except Exception as e:\n        return {\"cmd\":cmd,\"ok\":False,\"error\":str(e),\"latency_seconds\":round(time.time()-start,3)}\n\ndef audit():\n    checks=[]\n    for cmd in [\n        [sys.executable,\"seed_hygiene_status_v13623.py\"],\n        [sys.executable,\"seed_service_supervisor_v1371.py\",\"status\"],\n        [sys.executable,\"seed_runtime_proxy_v1371.py\",\"wake-text\",\"status\"],\n        [sys.executable,\"seed_log_optimizer_v1371.py\",\"status\"],\n    ]:\n        if Path(cmd[1]).exists():\n            checks.append(run(cmd))\n    files=[]\n    for pat in [\"*events.jsonl\",\"*.log\",\"seed_full_outputs_v1371/*.json\"]:\n        for p in glob.glob(pat):\n            path=Path(p)\n            if path.is_file():\n                files.append({\"path\":str(path),\"bytes\":path.stat().st_size})\n    files=sorted(files,key=lambda x:x[\"bytes\"],reverse=True)[:20]\n    problems=[]\n    for c in checks:\n        if not c.get(\"ok\"):\n            problems.append({\"type\":\"command_failed\",\"cmd\":c.get(\"cmd\"),\"error\":c.get(\"error\") or c.get(\"stderr_tail\")})\n        if c.get(\"latency_seconds\",0)>8:\n            problems.append({\"type\":\"slow_command\",\"cmd\":c.get(\"cmd\"),\"latency_seconds\":c.get(\"latency_seconds\")})\n        if c.get(\"stdout_bytes\",0)>20000:\n            problems.append({\"type\":\"huge_stdout\",\"cmd\":c.get(\"cmd\"),\"stdout_bytes\":c.get(\"stdout_bytes\")})\n    for f in files:\n        if f[\"bytes\"]>750000:\n            problems.append({\"type\":\"large_log\",\"path\":f[\"path\"],\"bytes\":f[\"bytes\"]})\n    score=max(0,100-len(problems)*8)\n    report={\"created_at\":now(),\"version\":VERSION,\"ok\":True,\"clunkiness_score\":score,\"problem_count\":len(problems),\"problems\":problems,\"checks\":checks,\"largest_files\":files}\n    REPORT.write_text(json.dumps(report,indent=4,ensure_ascii=False))\n    return report\n\ndef text(r):\n    lines=[f\"Seed v137.1 Clunkiness Audit\",f\"Score: {r['clunkiness_score']}/100\",f\"Problems: {r['problem_count']}\"]\n    for p in r[\"problems\"][:10]:\n        lines.append(\"- \"+json.dumps(p,ensure_ascii=False))\n    return \"\\n\".join(lines)\n\nif __name__==\"__main__\":\n    r=audit()\n    if \"--json\" in sys.argv:\n        print(json.dumps(r,indent=4,ensure_ascii=False))\n    else:\n        print(text(r))\n", "seed_companion_v1371.py": "import json, subprocess, sys, time\nfrom pathlib import Path\nfrom datetime import datetime\n\nVERSION=\"v137.1.0\"\nEVENTS=Path(\"seed_companion_v1371_events.jsonl\")\n\ndef now():\n    return datetime.now().isoformat(timespec=\"seconds\")\n\ndef event(row):\n    row.setdefault(\"created_at\",now())\n    row.setdefault(\"version\",VERSION)\n    with EVENTS.open(\"a\") as f:\n        f.write(json.dumps(row,ensure_ascii=False)+\"\\n\")\n    return row\n\ndef run(cmd, timeout=120):\n    try:\n        p=subprocess.run(cmd,capture_output=True,text=True,timeout=timeout)\n        data=None\n        if p.stdout.strip():\n            try: data=json.loads(p.stdout)\n            except Exception: data={\"raw\":p.stdout.strip()[-2000:]}\n        return {\"ok\":p.returncode==0,\"returncode\":p.returncode,\"data\":data,\"stdout\":p.stdout[-2000:],\"stderr\":p.stderr[-1000:]}\n    except Exception as e:\n        return {\"ok\":False,\"error\":str(e)}\n\ndef py(script,*args):\n    return [sys.executable,script,*args]\n\ndef optimize_settings():\n    changed={}\n    # Make v137 less chatty / less clunky.\n    if Path(\"seed_companion_v137_settings.json\").exists():\n        try:\n            s=json.loads(Path(\"seed_companion_v137_settings.json\").read_text(errors=\"ignore\"))\n        except Exception:\n            s={}\n    else:\n        s={}\n    defaults={\n        \"loop_interval_seconds\":3,\n        \"cooldown_seconds\":10,\n        \"audio_wake_enabled\":False,\n        \"speak_default\":False,\n        \"max_events\":600,\n        \"auto_start_services\":True,\n        \"start_autopilot\":True,\n        \"start_voice_runtime\":True,\n    }\n    for k,v in defaults.items():\n        if s.get(k)!=v:\n            changed[k]={\"old\":s.get(k),\"new\":v}\n            s[k]=v\n    s[\"version\"]=\"v137.1_optimized_profile\"\n    Path(\"seed_companion_v137_settings.json\").write_text(json.dumps(s,indent=4,ensure_ascii=False))\n    event({\"event\":\"optimize_settings\",\"changed\":changed})\n    return {\"ok\":True,\"changed\":changed,\"settings\":s}\n\ndef start(panel=False, all_services=False):\n    optimize_settings()\n    if Path(\"seed_service_supervisor_v1371.py\").exists():\n        sup=run(py(\"seed_service_supervisor_v1371.py\",\"start\",\"--all\" if all_services else \"\"))\n    else:\n        sup=run(py(\"seed_companion_v137.py\",\"start\"))\n    pan=None\n    if panel and Path(\"seed_companion_panel_v137.py\").exists():\n        pan=run(py(\"seed_companion_panel_v137.py\",\"start\"))\n    out={\"ok\":True,\"supervisor\":sup,\"panel\":pan,\"status\":status()}\n    event({\"event\":\"start\",\"panel\":panel,\"all_services\":all_services})\n    return out\n\ndef stop(all_services=False):\n    if Path(\"seed_service_supervisor_v1371.py\").exists():\n        return run(py(\"seed_service_supervisor_v1371.py\",\"stop\",\"--all\" if all_services else \"\"))\n    return run(py(\"seed_companion_v137.py\",\"stop\"))\n\ndef ask(text):\n    if Path(\"seed_runtime_proxy_v1371.py\").exists():\n        res=run(py(\"seed_runtime_proxy_v1371.py\",\"wake-text\",text,\"--json\"),timeout=240)\n        return res.get(\"data\") or res\n    if Path(\"seed_companion_v137.py\").exists():\n        run(py(\"seed_companion_v137.py\",\"enqueue\",\"wake up \"+text))\n        time.sleep(3)\n        return run(py(\"seed_companion_v137.py\",\"status\"))\n    return {\"ok\":False,\"error\":\"missing v137 runtime\"}\n\ndef status():\n    rows={}\n    for script,args,name in [\n        (\"seed_service_supervisor_v1371.py\",[\"status\"],\"supervisor\"),\n        (\"seed_companion_v137.py\",[\"status\"],\"companion\"),\n        (\"seed_hygiene_status_v13623.py\",[],\"hygiene\"),\n        (\"seed_log_optimizer_v1371.py\",[\"status\"],\"logs\"),\n    ]:\n        if Path(script).exists():\n            rows[name]=run(py(script,*args),timeout=90)\n    return {\"created_at\":now(),\"version\":VERSION,\"ok\":True,\"rows\":rows}\n\ndef daily():\n    out={\"created_at\":now(),\"version\":VERSION,\"optimize_settings\":optimize_settings()}\n    if Path(\"seed_log_optimizer_v1371.py\").exists():\n        out[\"log_plan\"]=run(py(\"seed_log_optimizer_v1371.py\",\"dry-run\"))\n    if Path(\"seed_service_supervisor_v1371.py\").exists():\n        out[\"heal\"]=run(py(\"seed_service_supervisor_v1371.py\",\"heal\"))\n    if Path(\"seed_clunkiness_audit_v1371.py\").exists():\n        out[\"audit\"]=run(py(\"seed_clunkiness_audit_v1371.py\",\"--json\"),timeout=240)\n    event({\"event\":\"daily\",\"ok\":True})\n    return out\n\ndef test():\n    return {\"ok\":True,\"optimize\":optimize_settings(),\"ask\":ask(\"status\"),\"daily\":daily()}\n\nif __name__==\"__main__\":\n    args=sys.argv[1:]\n    cmd=args[0] if args else \"status\"\n    if cmd==\"start\":\n        print(json.dumps(start(panel=\"--panel\" in args, all_services=\"--all\" in args),indent=4,ensure_ascii=False))\n    elif cmd==\"stop\":\n        print(json.dumps(stop(all_services=\"--all\" in args),indent=4,ensure_ascii=False))\n    elif cmd in {\"ask\",\"wake-text\"}:\n        print(json.dumps(ask(\" \".join(args[1:]) or \"status\"),indent=4,ensure_ascii=False))\n    elif cmd==\"daily\":\n        print(json.dumps(daily(),indent=4,ensure_ascii=False))\n    elif cmd==\"optimize\":\n        print(json.dumps(optimize_settings(),indent=4,ensure_ascii=False))\n    elif cmd==\"test\":\n        print(json.dumps(test(),indent=4,ensure_ascii=False))\n    else:\n        print(json.dumps(status(),indent=4,ensure_ascii=False))\n", "seed_v1371_optimization_gate.py": "import json, subprocess, sys\nfrom pathlib import Path\nfrom datetime import datetime\n\nMODULES=[\n    \"seed_output_compactor_v1371.py\",\n    \"seed_runtime_proxy_v1371.py\",\n    \"seed_log_optimizer_v1371.py\",\n    \"seed_service_supervisor_v1371.py\",\n    \"seed_clunkiness_audit_v1371.py\",\n    \"seed_companion_v1371.py\",\n    \"seed_v1371_optimization_gate.py\"\n]\n\ndef now():\n    return datetime.now().isoformat(timespec=\"seconds\")\n\ndef comp(m):\n    p=subprocess.run([sys.executable,\"-m\",\"py_compile\",m],capture_output=True,text=True,timeout=30)\n    return {\"module\":m,\"ok\":p.returncode==0,\"stderr\":p.stderr[-1000:]}\n\ndef run(cmd,timeout=180):\n    p=subprocess.run(cmd,capture_output=True,text=True,timeout=timeout)\n    data=None\n    if p.stdout.strip():\n        try: data=json.loads(p.stdout)\n        except Exception: data={\"raw\":p.stdout.strip()[-2000:]}\n    return {\"ok\":p.returncode==0,\"stdout\":p.stdout[-3000:],\"stderr\":p.stderr[-1000:],\"data\":data}\n\ndef run_gate():\n    checks=[comp(m) for m in MODULES]\n    modules_ok=all(c[\"ok\"] for c in checks)\n    details={}\n    proxy_ok=False; supervisor_ok=False; audit_ok=False; companion_ok=False\n    try:\n        details[\"proxy\"]=run([sys.executable,\"seed_runtime_proxy_v1371.py\",\"wake-text\",\"status\",\"--json\"],240)\n        proxy_ok=details[\"proxy\"].get(\"ok\") and isinstance(details[\"proxy\"].get(\"data\"),dict) and details[\"proxy\"][\"data\"].get(\"answer\") is not None\n    except Exception as e:\n        details[\"proxy_error\"]=str(e)\n    try:\n        details[\"supervisor\"]=run([sys.executable,\"seed_service_supervisor_v1371.py\",\"heal\"],180)\n        supervisor_ok=details[\"supervisor\"].get(\"ok\")\n    except Exception as e:\n        details[\"supervisor_error\"]=str(e)\n    try:\n        details[\"audit\"]=run([sys.executable,\"seed_clunkiness_audit_v1371.py\",\"--json\"],240)\n        audit_ok=details[\"audit\"].get(\"ok\")\n    except Exception as e:\n        details[\"audit_error\"]=str(e)\n    try:\n        details[\"companion\"]=run([sys.executable,\"seed_companion_v1371.py\",\"optimize\"],120)\n        companion_ok=details[\"companion\"].get(\"ok\")\n    except Exception as e:\n        details[\"companion_error\"]=str(e)\n    report={\n        \"created_at\":now(),\"version\":\"v137.1.0\",\n        \"ready\":modules_ok and proxy_ok and supervisor_ok and audit_ok and companion_ok,\n        \"modules_ok\":modules_ok,\"proxy_ok\":proxy_ok,\"supervisor_ok\":supervisor_ok,\"audit_ok\":audit_ok,\"companion_ok\":companion_ok,\n        \"checks\":checks,\"details\":details\n    }\n    Path(\"seed_v1371_optimization_gate_report.json\").write_text(json.dumps(report,indent=4,ensure_ascii=False))\n    return report\n\ndef show():\n    r=run_gate()\n    print(\"\\n=== SEED v137.1 BIG OPTIMIZATION / DE-CLUNK PACK GATE ===\")\n    print(f\"Ready: {r['ready']}\")\n    print(f\"Modules OK: {r['modules_ok']}\")\n    print(f\"Proxy OK: {r['proxy_ok']}\")\n    print(f\"Supervisor OK: {r['supervisor_ok']}\")\n    print(f\"Audit OK: {r['audit_ok']}\")\n    print(f\"Companion OK: {r['companion_ok']}\")\n    try:\n        ans=(r[\"details\"][\"proxy\"][\"data\"].get(\"answer\") or \"\")[:140]\n        print(\"Compact answer preview:\", ans)\n    except Exception:\n        pass\n\nif __name__==\"__main__\":\n    show()\n"}
for path,text in FILES.items():
    Path(path).write_text(text)
    print("Wrote",path)

cfgp=Path("seed_config.py")
cfg=cfgp.read_text(errors="ignore") if cfgp.exists() else 'SEED_VERSION = "v137.1.0"\n'
cfg=re.sub(r'^SEED_VERSION\s*=\s*".*?"','SEED_VERSION = "v137.1.0"',cfg,flags=re.M)
for name in ["SEED_V1371_OUTPUT_COMPACTOR","SEED_V1371_RUNTIME_PROXY","SEED_V1371_LOG_OPTIMIZER","SEED_V1371_SERVICE_SUPERVISOR","SEED_V1371_CLUNKINESS_AUDIT"]:
    if name not in cfg:
        cfg += f"\n{name} = True"
cfgp.write_text(cfg+"\n")
print("Updated seed_config.py")

core=Path("Seed_Core.md")
ct=core.read_text(errors="ignore") if core.exists() else ""
if "Seed v137.1 Big Optimization / De-Clunk Pack" not in ct:
    ct += "\n## Seed v137.1 Big Optimization / De-Clunk Pack\n\nAdds compact runtime output, full-output archiving, runtime proxy, log optimizer, service supervisor/healer, clunkiness audit, optimized companion wrapper, and v137.1 gate. Goal: make Seed less clunky without changing the stable v137 base.\n"
core.write_text(ct)
print("Updated Seed_Core.md")

gi=Path(".gitignore")
g=gi.read_text(errors="ignore") if gi.exists() else ""
block="\n# Seed v137.1 optimization runtime\nseed_output_compactor_v1371_events.jsonl\nseed_runtime_proxy_v1371_events.jsonl\nseed_log_optimizer_v1371_report.json\nseed_log_optimizer_v1371_events.jsonl\nseed_log_archive_v1371/\nseed_service_supervisor_v1371_events.jsonl\nseed_service_supervisor_v1371_state.json\nseed_clunkiness_audit_v1371_report.json\nseed_companion_v1371_events.jsonl\nseed_v1371_optimization_gate_report.json\nseed_full_outputs_v1371/\n"
if "Seed v137.1 optimization runtime" not in g:
    gi.write_text(g+block)
    print("Updated .gitignore")

for m in FILES:
    p=subprocess.run([sys.executable,"-m","py_compile",m],capture_output=True,text=True,timeout=30)
    print("$ python -m py_compile",m)
    if p.returncode==0:
        print("OK")
    else:
        print(p.stderr)
        sys.exit(p.returncode)

for c in [[sys.executable,"seed_companion_v1371.py","test"],[sys.executable,"seed_v1371_optimization_gate.py"]]:
    p=subprocess.run(c,capture_output=True,text=True,timeout=300)
    print("$"," ".join(c))
    print(p.stdout.strip()[:7000])
    if p.returncode!=0:
        print(p.stderr)
        sys.exit(p.returncode)

print("\nSeed v137.1 Big Optimization / De-Clunk Pack installed.")
